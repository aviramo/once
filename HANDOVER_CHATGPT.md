# Once — קובץ חפיפה מלא (סנכרון ל-ChatGPT)

> מסמך יחיד, עצמאי, שמסביר **מה האפליקציה עושה** — המוצר, המצבים, הכללים, השרת, הלקוח והאתר.
> מיועד לסנכרון עוזר חיצוני (ChatGPT) שאין לו גישה לקוד.
> גרסה: **2026-07-27 (עדכון ב')** · נכתב מול הקוד ב-`HEAD` **ומול השינויים שבעץ העבודה**,
> מול תוכנית ה-QA ומול פונקציות ה-DB החיות.
> מה השתנה מאז הגרסה הקודמת מרוכז ב-**§16** בסוף.

---

## 1. במשפט אחד

**Once** היא אפליקציית היכרויות שבה כל משתמש נמצא **עם אדם אחד בלבד בכל רגע נתון**.
אין רשימת התאמות, אין גלילה אינסופית של פרופילים, ואין "לייקים" שנצברים.
המשתמש רואה מועמד אחד, מזמין אותו לשיחה, וההזמנה חיה **10 דקות**. אם היא אושרה —
נפתחת שיחה פרטית אחת. עד שהיא מסתיימת, **שני הצדדים מוסתרים מכל השאר**.

- פלטפורמה: **Android בלבד כרגע** (הקוד תומך גם ב-iOS; לא מפורסם).
- שפות: **עברית (ברירת מחדל) ואנגלית**, כולל RTL מלא. אין רוסית.
- קהל: ישראל בעיקר; ההודעות מגונדרות לפי מין (זכר/נקבה) בשני הצדדים.

---

## 2. הסטאק

| שכבה | טכנולוגיה |
|---|---|
| מובייל | React Native + Expo (Expo Router), New Architecture (Fabric) פעיל |
| שרת | Supabase Edge Functions (Deno) — פונקציה אחת: `/app/<action>` |
| DB | Postgres (Supabase). כל טרנזיציית מצב = **RPC יחיד** (`app_*`) |
| Realtime | Supabase Realtime (עדכוני `users.relations` + טבלת `chat` + presence) |
| Storage | באקטים: תמונות פרופיל, `chat-images`, אודיו צ'אט, `bug-attachments` |
| פוש | Expo Push (טוקן Expo נשמר על המשתמש) |
| קרון | pg_cron: `expire-sweep` (כל דקה), `watch-expire-sweep` (כל שעה), סוויפ תגמולים, סוויפ תזכורת לבקשות הצטרפות |
| מטמון מקומי | AsyncStorage — פרופיל המשתמש, תמלילי צ'אט, קבוצות, רשימות אנשים |
| אתר | Next.js (`web/`) — דפי נחיתה סטטיים + **פאנל אדמין** |

**אין סביבת staging.** פרויקט Supabase אחד משרת גם ייצור וגם בדיקות. ההפרדה היחידה
היא דגל `users.is_test` / `groups.is_test` (ראה §12).

---

## 3. מודל הנתונים המרכזי

כל לוגיקת המשחק יושבת ב-**עמודה אחת**: `users.relations` (JSONB). זה הלב של המערכת.

```
users
├─ user_id, name, birth_date, is_male, images, bio (ב-data)
├─ location / location_custom / location_type / location_label
├─ last_seen           ← חלון נוכחות
├─ is_test             ← מחיצת בדיקות
├─ referral_code       ← קוד הזמנה אישי (4–16 אלפאנומרי)
└─ relations (JSONB)
   ├─ page1        ← "מה אני עושה" — הלוח היוזם
   ├─ page2        ← "מה עושים איתי" — הלוח המקבל
   ├─ credits      ← { balance, extra, held, next_grant_at, unpaid_at }
   ├─ availability ← { state, reason }
   ├─ push         ← { perm, token, dead }
   ├─ referral     ← { joined, ... }
   └─ last_add_at  ← throttle לברודקאסט
```

> ⚠️ **`page1`/`page2` הם מונחי מודל-נתונים בלבד — הם *לא* מסכים.** באפליקציה יש מסך אחד.

### 3.1 `page1` — מכונת המצבים של היוזם
| מצב | משמעות |
|---|---|
| `free` | פנוי, אין מועמד. לחיצת play תחפש. |
| `watching` | מוצג לו מועמד ספציפי (יש `profile`). |
| `waiting` | שלח הזמנה, ממתין לתשובה (`expires_at` חי, קרדיט מוחזק). |
| `chat` | בשיחה פעילה. |
| `locked` | חסום זמנית עם `message` שמסביר למה (`invite-fail`, `expired-out`, `cancel`, `left`, `logout`, `delete`...). כרטיס מצב "מת" שדורש ניקוי ידני (`clear1`). |

### 3.2 `page2` — מכונת המצבים של המקבל
| מצב | משמעות |
|---|---|
| `free` | גלוי, עם `profiles[]` = רשימת ה"צופים" (מי מסתכל עליי כרגע). |
| `waiting` | יש הזמנה נכנסת ממתינה. |
| `chat` | בשיחה. |
| `locked` | מוסתר. עם `message` = נסגר ע"י אירוע (`logout`, `decline`, `remove`); בלי `message` = **המשתמש הסתיר את עצמו ידנית** (נשמר בין התחברויות). |

### 3.3 טבלאות נלוות
`chat` (הודעות) · `chat_reads` (אישורי קריאה) · `restrictions` (חסימות זמניות/קבועות) ·
`log` (שורה לכל בקשת HTTP) · `groups` / `user_groups` / `group_join_requests` ·
`friend_links` (חברות, מאוחסנת `a=LEAST, b=GREATEST`) · `referrals` (שיוכי הזמנה) ·
`reports` (דיווחי משתמשים) · דיווחי באג.

עמודות חדשות שכדאי להכיר: `user_groups.hidden` (חבר שביקש לא לפגוש את הקבוצה שלו
במשחק — §6.3) · `group_join_requests.nudged_at` (חותמת התזכורת לצוות — §6.4) ·
`groups.is_test` (צד המחיצה של הקבוצה, נחתם בטריגר — §12).

---

## 4. זרימת המשתמש המלאה

### 4.1 הגעה והתקנה
ארבע דרכי כניסה, כולן מגיעות לאותו אונבורדינג:
1. **הורדה אורגנית** מ-Play — בלי שיוך.
2. **קישור הזמנה אישי** `https://once-lake.vercel.app/i/<CODE>` — משייך את המתקין למזמין
   דרך Google **Install Referrer**; המזמין מקבל **+1 קרדיט** אחרי שהמוזמן מעלה תמונה ראשונה.
   השיוך שקוף לחלוטין למוזמן (הוא לא רואה שום אזכור לקוד).
3. **קישור חבר** `/f/<CODE>` — יוצר **חברות הדדית מיידית בלי אישור** בין השניים.
   אם האפליקציה לא מותקנת → `/download?ref=<CODE>&f=1` ואז גם שיוך קרדיט **וגם** חברות.
4. **קישור קבוצה** `/g/<TOKEN>` (או הקלדת קוד בן 6 ספרות) — מצרף לקבוצה, מיד או בהמתנה לאישור.

דיפ-לינק שמגיע כשהאפליקציה **פתוחה לא מנווט ולא מרענן** — הצ'אט הפתוח נשאר פתוח, הטיוטה נשמרת.

### 4.2 הזדהות ועלייה של האפליקציה
Google OAuth · Apple (iOS בלבד) · **קישור קסם באימייל** (magic link) ·
מסלול מיוחד לחשבון **בדיקת חנות** (כתובת קבועה חושפת שדה קוד).
סשן נשמר; טיים-אאוט של 5 שניות בעליית סשן נופל למסך התחברות במקום להיתקע.

**עלייה מיידית מהדיסק (stale-while-revalidate).** הפרופיל של המשתמש עצמו נשמר
לדיסק בכל עדכון, ובעלייה נקראים במקביל **גם** הדיסק **וגם** השרת: מה שנוחת ראשון מצייר,
והתשובה מהשרת תמיד מנצחת. זה מה שמאפשר להפעלה חוזרת להגיע ל-home (ולהרכיב את שכבת
הצ'אט, שקוראת את התמליל המקומי שלה) בלי להמתין לסבב הרשת. יציאה מהחשבון מוחקת
את המטמון הזה יחד עם כל שאר המטמונים המקומיים.

### 4.3 אונבורדינג — 5 שלבים
1. **מין** → 2. **שם** (עד 30 תווים) → 3. **תאריך לידה** (18–120)
   ← *כאן החשבון נוצר בשרת* (`app/account`)
4. **תמונות** — מינימום 2, מקסימום 6 (הראשונה היא האווטר)
5. **ביו** — 20–150 תווים, שורות ריקות נמחקות אוטומטית

משתמש שעצר אחרי שלב 3 הוא **browse-only**: הוא רואה מועמדים אבל לא יכול להזמין
(השרת מחזיר `profile_incomplete` 403), ולא מופיע אצל אחרים.

### 4.4 שערי זמינות (Gates)
לפני שהמשחק זמין, המשתמש חייב לעבור:
- **הרשאת התראות** — דחייה או טוקן מת (`push.dead`) ⇒ `availability = {unavailable, push}`.
  המשתמש לא מוצג לאף אחד ולא יכול `find`/`invite`/`approve`. **התפריט אף פעם לא נחסם.**
- **הרשאת מיקום** — בלי `location` הוא לא מוצג לאחרים (אבל כן יכול לצפות).
- **חיבור רשת**.
- **חלון נוכחות 24 שעות** — `last_seen` ישן מיום ⇒ יוצא מהפול. פתיחת האפליקציה מחזירה מיד.
- **`unpaid_at`** — מי שלחץ אישור בארנק ריק מסומן, ואף אחד לא יכול להזמין אותו עד שיהיה לו קרדיט.

*(שערים גאוגרפיים — "האפליקציה זמינה באזור שלך" — הוסרו. `push_blocked()` הוא השער היחיד.)*

### 4.5 המשחק — מציאה
לחיצת **play** ⇒ `app/find` ⇒ הפונקציה `others()` מחזירה מועמד יחיד.
`others()` היא **נקודת החנק היחידה** של כל מועמדות במערכת. הקריטריונים:

- התאמת מין **דו-כיוונית** (אני מחפש אותו *וגם* הוא אותי).
- טווח גילים דו-כיווני.
- מרחק — הטווח האפקטיבי הוא ה-`LEAST` של שני הצדדים; "ללא הגבלה" = בלי סינון מהצד הזה.
- לפחות **תמונה אחת** ו-`location` קיים.
- `last_seen` בתוך 24 שעות.
- לא נעול (לא בצ'אט, לא בהזמנה חיה, לא מוסתר).
- אין `restriction` פעילה בין השניים.
- **אין ביניהם קבוצה משותפת שבה אחד מהם מסומן `hidden`** (ראה §6.3) — הדרה סימטרית.
- **`other.is_test = me.is_test`** — מחיצת הבדיקות.

**בוסטים לרלוונטיות:** קבוצה משותפת **×3** · חברות **×3** · ברודקאסט טרי (30 דק') **×2**.
בנוסף מיון לפי התאמת העדפות ילדים וחפיפת לוחות זמנים.

חיפוש אוטומטי רץ גם ב-`start` / `focus` / `location` כשהמצב `free` — אבל **לעולם לא דורס** מצב פעיל.

### 4.6 דילוג וצופים
- **גרירת הכרטיס למטה = דילוג** (`app/ignore`) ⇒ הכרטיס מחליק החוצה, מיד מתחיל חיפוש הבא,
  ונוצרת הגבלה של **יום**.
- **pause** (כפתור המרכז) עוצר את החיפוש; גם אם השרת כבר מצא מועמד, הוא לא יוצג.
- **צופים**: כשמישהו רואה אותי, הוא נכנס ל-`page2.profiles`. משתמש חדש בלי צופים מקבל
  צופה מוזרע ע"י השרת (`app_seed_viewer`), והצופה מקבל פוש `candidate`.

### 4.7 הזמנה (הלב של המוצר)
לחיצה על **הלב** בכרטיס ⇒ פופאפ ⇒ שליחה (`app/invite`):

- **עולה 1 קרדיט**, שנכנס למצב **`held`** (מוחזק, לא מבוזבז).
- `page1` → `waiting`, `expires_at = now + 10 דקות`.
- הנמען מקבל פוש `invite-in` וכרטיס הזמנה נכנסת עם **טיימר חי**.
- **שני הצדדים נעולים** — לא מקבלים ולא שולחים הזמנות אחרות, ולא מופיעים בפול.

תוצאות אפשריות:

| תוצאה | פעולה | קרדיט המזמין | הגבלה |
|---|---|---|---|
| **אישור** | `app/approve` (עולה 1 לנמען) | נצרך | — |
| **דחייה** | `app/decline` | **מוחזר** | 7 ימים |
| **ביטול** (ע"י המזמין) | `app/cancel` | **מתאבד** (לא מוחזר) | יום |
| **פג תוקף** | קרון / `app_expire_self` | **מוחזר** | — |
| **מיגזע** (מישהו הקדים) | — | לא יורד / מוחזר | — |

הכרטיסים ה"מתים" (פג תוקף, בוטל, נדחה, מישהו הקדים) מוצגים עם טקסט מגונדר לפי מין
**שני** הצדדים, וכפתור יחיד שמנקה ומחזיר לזמינות.

התפוגה מטופלת בשלושה מנגנונים במקביל: קרון דקתי, `app_expire_self` מיידי מהקליינט
בזמן `focus`, וגם lazy-expire בקריאה. השרת **בודק שוב מתחת ללוק** — שעון מכשיר מקדים
לא מצליח לסגור הזמנה חיה.

### 4.8 צ'אט
שיחה אחת בלבד, בין שני משתמשים, עד שאחד מהם מסיים.

**סוגי תוכן נתמכים:**
| סוג | פרטים |
|---|---|
| טקסט | עד 10 שורות בשדה, אז גולל |
| תמונה | באקט `chat-images` תחת `<uid>/`, URL חתום ל-24 שעות, מטמון מקומי |
| **אודיו** | הקלטה עם ויזואליזציה חיה; בועה עם **גל של 60 עמודות** + משך; נגינה רציפה עם מעבר אוטומטי להודעה הבאה |
| **מיקום** | `{lat,lng}`; לחיצה פותחת אפליקציית מפות |
| **לוח זמנים** | **רק להורים** (`data.family.hasKids`). בורר 1–4 שבועות × 7 ימים, נשלח כתצלום קפוא. אחרת `schedule_not_allowed` 403 |
| **reply** | ציטוט של הודעה קודמת (preview עד 140 תווים); לחיצה גוללת למקור |

**פעולות על הודעה — שתי דרכים לאותו דבר:**
- **החלקה אופקית** על בועה = מענה (הבועה זזה וחץ מענה נחשף מאחוריה).
- **לחיצה ארוכה** (400ms) על בועה פותחת גיליון פעולות: **מענה** או **העתקת הטקסט**.
  שתי המחוות מתחרות זו בזו (`Gesture.Race`): אצבע שנעה = החלקה, אצבע שנחה = לחיצה ארוכה.
  ההחלקה קלה לפספס וקל לאבד אותה לגלילה, ולכן הלחיצה הארוכה היא הדרך המפורשת.
  ההעתקה עוברת דרך מודול קליפבורד יחיד (RN core — שינוי JS בלבד, בלי בילד נייטיב חדש).

**סנכרון:** Realtime ראשי + **פולינג גיבוי** אם ה-WS נופל, עם דדופ.
בועה אופטימיסטית מיד → V אחת (נשמר) → V כפולה (נקרא, דרך `chat_reads`).
היסטוריה: 100 הודעות ראשונות, גלילה למעלה טוענת עוד.
**"מקליד/ה..."** הוא **כרום ולא הודעה** — שלוש הנקודות צפות על שורת הכותרת של השכבה,
ליד ה-X, ולא נכנסות לרשימה בכלל. שום דבר בהן לא יכול להזיז בועה. הן נעלמות אחרי
3 שניות בלי אות חדש, ומיד כשמגיעה הודעה מהצד השני או כשהוא מתנתק.
אינדיקציית "מחובר" דרך presence.

**התמליל המקומי (מטמון הצ'אט):**
- נכתב לדיסק ב-debounce של 800ms, ובנוסף **נשטף מיד כשהאפליקציה יוצאת מהחזית**
  (תהליך שה-OS הורג ברקע לא יאבד את ההודעה האחרונה).
- בעלייה הוא מצייר את ההיסטוריה **רק אם הרשימה עדיין ריקה** — תשובת שרת או הודעה
  אופטימיסטית שכבר על המסך תמיד מנצחות את הדיסק.
- **נמחק ברגע שהשיחה נגמרת** (המצב יוצא מ-`chat`) ובכל יציאה מהחשבון, כדי שלא יצטברו
  תמלילים מתים על המכשיר. השורות נשארות בשרת, אז זוג שיתאים שוב פשוט טוען מחדש.

### 4.9 סיום
| פעולה | תוצאה | הגבלה |
|---|---|---|
| **סיום שיחה** (`app/leave`) | שני הצדדים יוצאים; פוש `left`; **אין החזר** | 14 יום |
| **חסימה** (`app/block`) | הזוג לא רואה זה את זה שוב לעולם | לנצח |
| **דיווח** (`app/report`) | רשומת תלונה לאדמין + **חסימה לצמיתות** + פירוק הקשר | לנצח |
| **הסרת צופה** (`app/remove`) | הצופה מקבל "כבר לא זמין/ה לך" | יום |
| **הסתרה** (`lock2`) | מעיף את כל הצופים | — |

הדיווח זמין מכל משטח: צפייה, הזמנה יוצאת, הזמנה נכנסת, צ'אט, וכרטיס מצב מת.
**לא** זמין בתצוגת הפרופיל של עצמי.

**טבלת חלונות ההגבלה (מרוכזת):**

| מקור | חלון |
|---|---|
| `ignore` (דילוג) / `cancel` / `remove` | 1 יום |
| `decline` (דחיית הזמנה) | 7 ימים |
| `leave` (סיום שיחה) | 14 יום |
| `block` / `report` | לנצח |

כל ההגבלות **דו-כיווניות**.

---

## 5. כלכלת הקרדיטים

- **מכסה יומית: 1 קרדיט**, מתחדש ב-**20:00 שעון ישראל** (`next_grant_at`). תקרה קשה — לא נצבר.
- **ארנק:** `balance` (יומי) + `extra` (קנוי/מתגמול) + `held` (מוחזק בהזמנה חיה).
- **עלויות:** הזמנה = 1 · אישור = 1 · ברודקאסט = 1 · ביטול = 0 (הקרדיט המוחזק מתאבד).
- **החזרים:** דחייה ותפוגה מחזירים; החזר **לא חורג מהתקרה** (קרדיט יומי חוזר ל-`balance`, קנוי ל-`extra`).
  אי אפשר לצבור קרדיטים ע"י מחזור שליחה+תפוגה.
- **רכישה בכסף מושבתת** — האפשרויות 3/10/50 מוצגות עם תג **"בקרוב"**.
- **הדרך היחידה להשיג קרדיט נוסף היום: הזמנת חבר** (`/i/<CODE>`), +1 לכל מוזמן שהשלים פרופיל.
- `unpaid_at` — מי שניסה לאשר בארנק ריק מסומן ויוצא מהפול עד שיגיע קרדיט; הסימון מתנקה אוטומטית.

**איך זה מוצג בהגדרות (עודכן 2026-07-27):** שורת "קרדיטים" היא טבלה קטנה בת **שתי שורות,
שורה לכל בריכה** — התיאור בצד ההתחלה והכמות בצד הסוף, כך שהמספרים מיושרים בעמודה אחת:

```
קרדיטים
  יומי, מתחדש ב-20:00            1/1
  אקסטרה שנשארים איתך              5
```

- שעת החידוש רוכבת **בתוך שורת היומי בלבד** (היא לא חלה על האקסטרה, שלא פגים).
  אם השעה לא ידועה, הפסוקית נופלת ונשארת "יומי".
- **בלי אקסטרה השורה הופכת להזמנה לפעולה** — "לחץ/י להוספת אקסטרה" (השורה כולה
  פותחת ממילא את בורר הקרדיטים), במקום "0" חשוף בלי מה לעשות איתו.
- הזמן מוצג כשעה חשופה "HH:MM" ולא כ"היום/מחר ב-" — הבריכה מתחדשת בשעה הזאת **כל יום**,
  ומילת יום תיארה מופע יחיד של כלל קבוע (ואחרי השעה אף הטעתה).

---

## 6. מעגלים, קבוצות וחברים

> **שינוי מינוח (2026-07-27):** מה שנקרא בעבר "קהילות" נקרא בממשק ובאתר **"מעגלים"**
> (Circles). המפתחות בקוד עדיין `communities.*` והפעולות בשרת עדיין `*_group*` —
> רק הטקסט למשתמש השתנה.

### 6.1 חברים
- **רשימת "החברים שלי"** בתפריט → מעגלים.
- שתי דרכים להתחבר: **קישור `/f/<CODE>`** (מיידי, בלי אישור) או **חיפוש + בקשה** (דורש אישור).
- חיפוש אנשים לפי שם, מינימום 2 תווים.
- חברות נותנת **בוסט ×3** ברלוונטיות.
- פושים: `friend_request`, `friend_accept`, `friend_link`.

### 6.2 קבוצות
- **תפקידים:** בעלים · מנהלים (מרובים) · חברים.
- **הגדרות קבוצה:** שם (חובה, עד 60 תווים), תיאור, **ציבורית/פרטית**, **דורשת אישור או לא**.
- **הצטרפות:** קישור `/g/<TOKEN>` · קוד בן 6 ספרות · חיפוש קבוצות ציבוריות (ממוין לפי גודל).
- קבוצה עם אישור ⇒ מצב `pending`, פוש `group_join` **לבעלים ולכל המנהלים**, אפשרות
  לבטל בקשה. אישור ⇒ פוש `group_approved` למבקש.
- **ניהול:** רשימת חברים (למנהלים בלבד, עם תור הממתינים בראשה), הסרת חבר (אי אפשר להסיר
  בעלים), מינוי/ביטול מנהל (בעלים בלבד), עריכה, רענון קוד הזמנה, מחיקת קבוצה (בעלים בלבד).
- **קבוצה משותפת נותנת בוסט ×3**, ומופיע צ'יפ "קבוצה משותפת" בכרטיס הפרופיל;
  לחיצה עליו פותחת פופאפ עם כל הקבוצות המשותפות.
- אין דרישה להיות בקבוצה כדי לשחק.

### 6.3 "אני מנהל אותה, אני לא משחק בה" (`user_groups.hidden`)
בעלים/מנהל יכול לסמן **את עצמו** כמוסתר **בתוך קבוצה מסוימת**:

- הדגל שייך ל**חברוּת** (האדם הזה, בקבוצה הזאת) — לא לקבוצה ולא לחשבון. באותו משתמש
  שום דבר לא משתנה בכל שאר הקבוצות ובכל שאר המשחק.
- ההדרה **סימטרית**: הוא לא יפגוש חברי קבוצה והם לא יפגשו אותו (`others()`).
- חברות מוסתרת גם **לא יכולה לתת את הצ'יפ** "קבוצה משותפת" ולא נספרת בפופאפ שלו.
- **הפעלת הדגל תופסת מיד**, לא רק בפיק הבא: זוגות שכבר "חונים" זה על הכרטיס של זה
  משוחררים בשקט — אבל **רק במצבים לא-מחייבים** (צפייה, מקום ברשימת צופים).
  הזמנה חיה או צ'אט פתוח **לא נקטעים**; הזוג פשוט לא ייפגש שוב אחריהם.
- **מה שבמפורש לא מוסתר:** המשטחים של הקבוצה עצמה. הרוסטר (לצוות) עדיין מציג אותו,
  וחיפוש קבוצות עדיין מציג את הבעלים ליד הקבוצה. לנהל קהילה בפומבי זה ההפך מלהסתתר —
  הדגל עוסק במשחק, לא בתפקיד.
- פעולה: `POST /app/set_group_hidden` → `app_set_group_hidden`.

### 6.4 תזכורת לתור הממתינים
בקשת הצטרפות שאיש לא ענה לה גרועה מבקשה שנדחתה. אחרי **48 שעות** הצוות מקבל
**תזכורת אחת לקבוצה** (פוש `group_pending`), והשורות מקבלות חותמת `nudged_at`
כך שאותה המתנה לא מזכירה פעמיים. נשלח מהסוויפ הדקתי (`ext`), עם `app_join_nudge_sweep`
שמסמן ובונה את רשימת ההתראות בפעולה אחת — כדי שהסוויפ יהיה אידמפוטנטי.

### 6.5 הניווט בתוך המעגלים (UI)
- **כל דף במעגלים נסגר כלפי מטה, ולכן על כל דף יש X** — גם על ההאב וגם על דף פנימי
  (שם ה-X מחליק דף אחד החוצה ומקפיץ אותו מהמחסנית). חץ "חזרה" שמור למשטח שיוצא הצידה,
  כלומר רק למגירת התפריט.
- **דף פרופיל הוא כרטיס במלוא המסך** — בלי סרגל כותרת, רק ה-X צף על התמונה.
- **שם קבוצה מוצג במלואו** — הכותרת גדלה כלפי מטה כמה שורות שצריך, השורה הראשונה
  והכפתורים נשארים במקום, ואין שלוש נקודות.
- **שורת מטא-דאטה ("ציבורית · 18 חברים · לא מקבלת בקשות")** נשברת לשתי שורות במקום
  להיחתך, והנקודה המפרידה **יורדת עם העובדה שאחריה** ולא נשארת תלויה בסוף שורה.
- בדף קבוצה שאני מנהל יש **שני כפתורים חסרי מילים** זה לצד זה מתחת לכפתור השיתוף:
  **גלגל שיניים** = הגדרות הקבוצה, **עין** = ניהול בלי לשחק (עין פקוחה = משחק כאן,
  עין חצויה = מוסתר). העין לבדה נושאת את המצב, בלי מילוי סגול שמסמן "דלוק".

---

## 7. חוזה ה-API (השרת)

**נקודת קצה אחת:** `POST /app/<action>`. כל פעולה → **RPC יחיד** `app_<action>`.

**רשימת ה-actions המלאה:**

```
account · start · location · focus · notif · age · range · preferred_gender
find · ignore · invite · approve · decline · cancel · leave · block
clear1 · clear2 · free2 · lock2 · pause · add · cancel_add · resume · extend · remove
profile · logout · delete · chat · report · bug_report
set_tier · buy_extra · referral
redeem_invite · my_groups · owned_groups · create_group · update_group · delete_group
group_members · remove_member · set_manager · set_group_hidden
group_requests · respond_join · leave_group · cancel_join · search_groups
my_friends · search_people · friend_request · friend_respond · unfriend · friend_link
shared_groups
```

**שערים:**
- `requiresPresence = [find, invite, add, approve]` — נחסמים כשהמשתמש לא זמין.
  `clear1/clear2/decline/cancel/leave/free2/lock2/pause/logout/ignore` **לא** נחסמים
  (המשתמש חייב תמיד יכולת לצאת ממצב תקוע).
- `requiresProfile = [invite, add]` — דורשים תמונה + ביו.

### 7.1 עקרונות שרת מחייבים
1. **מסלול קריטי קצר** — `await` רק על: אימות, הטרנזקציה האטומית, ובניית גוף התשובה.
   כל השאר (לוגים, פוש, כתיבות נגזרות) רץ ב-`EdgeRuntime.waitUntil(...)`.
   **פוש הוא תמיד fire-and-forget** — כשל בפוש לא מעכב ולא מכשיל את התשובה.
2. **טרנזיציה = RPC אחד** — `BEGIN; SELECT ... FOR UPDATE ordered by user_id; UPDATE; COMMIT`.
   הנעילה הממוינת מונעת דדלוקים ומחליפה לולאות retry אופטימיות.
3. **אינווריאנט הלוג** — **כל בקשת HTTP כותבת בדיוק שורת `log` אחת**, כולל דחיות ו-403.
   כל הגישה ל-DB עוברת דרך `Tools.invoke(...)`; קריאה ישירה ל-Supabase עוקפת את הלוג ואסורה.
4. **תאימות לאחור** — האפליקציה מתעדכנת בקצב של ימים-שבועות, השרת נפרס מיידית ל-100%.
   כל שינוי שובר מבוצע **Expand → Migrate → Contract** עם רישום ב-`BACKWARD_COMPAT.md`.
   נקודות קצה ישנות (`set_tier`, `units`, `join_request`) נשארות no-op כדי לא לשבור בילדים ותיקים.
   דוגמה טרייה: `user_groups.hidden` הוא תוספת טהורה — בילד ישן פשוט לא יכול להדליק אותה.

---

## 8. התראות פוש

הכותרת היא תמיד **שם השולח** (או שם הקבוצה, בהתראות קבוצה); הגוף הוא טקסט המצב,
מגונדר לפי מין, **בשפת הנמען**.

**קטלוג הקודים:**
`invite-in` · `candidate` · `match` · `declined` · `expired-out` · `expired-in` ·
`cancelled-in` · `removed` · `left` · `extended` · `invite-fail` · `approve-fail` ·
`chat` · `referral` · `friend_request` · `friend_accept` · `friend_link` ·
`group_join` (בקשה חדשה, עם שם הקבוצה) · `group_approved` (אושרת, עם שם הקבוצה) ·
**`group_pending`** (תזכורת לצוות על בקשה שממתינה 48 שעות).

**מדיניות:**
- **דיכוי מוחלט בחזית** — כשהאפליקציה פתוחה אין באנר/צליל/באדג'; העדכון מגיע ב-Realtime בלבד.
- **ניקוי מגש** בכל פתיחה/חזרה לחזית.
- קיפול (`collapseId`) לכמה התראות מאותו מקור.
- לחיצה על פוש פותחת את היעד הנכון (צ'אט פתוח, כרטיס הזמנה, מסך הקבוצה) — גם בקר-סטארט.
- טוקן שחוזר `DeviceNotRegistered` ⇒ `push.dead = true` ⇒ המשתמש יוצא מהפול.

---

## 9. ארכיטקטורת ה-UI במובייל (חוקי ברזל)

> זה החלק הכי קל לשבור בטעות. הכללים האלה מוחלטים.

**מסך אחד: `home`.** אין טאבים, אין pager, אין ניווט אופקי. כל השאר עולה כשכבה
מלאה (`OverlaySheet`) מעל הבית ונסגר **בגרירה למטה**.

**ארבע שכבות בלבד:** תפריט (מגירה מקצה ההתחלה) · תצוגה מקדימה של הפרופיל (נערמת מעל
התפריט) · צ'אט · כרטיס הזמנה.
**סדר צביעה:** `בית < הזמנה < צ'אט < תפריט < תצוגת פרופיל` — התפריט תמיד מעל הכול,
כי חייבים להגיע להגדרות גם כשהמשחק חסום.

**מימוש יחיד לגרירה** — קומפוננטה אחת (`PullPane`) מפעילה את *כל* מחוות הגרירה:
דילוג בבית, דחיית הזמנה, סגירת כל שכבה. אין מימוש שני.

### 9.1 שלושת חוקי הגרירה (עודכנו 2026-07-27, מוחלטים)

1. **הגלילה הפנימית תמיד מנצחת את הגרירה.** אצבע על תוכן שאפשר לגלול — מגללת אותו.
   הגרירה לוקחת את המשטח **רק** במקום שבו לגלילה לא נשאר מה לתת, והיא משתלטת
   **באמצע המחווה**: ברגע שהרשימה נגמרת, נקודת האפס של הגרירה נקבעת מחדש שם, כך
   שגרירה אחת גוללת עד למעלה וממשיכה ישר לסגירה. הרצועה שכן מרשה להתחיל גרירה
   בלי קשר לגלילה (`headerBottom`) מותרת **אך ורק** מעל כרום שלא נגלל — סרגל כותרת
   מלא, ראש קבוע מעל רשימה. לעולם לא מעל כותרת שצפה מעל תוכן נגלל, ולעולם לא מעל
   פיסת גלילה.
2. **שום דבר לא מצייר מחדש רק כי התחילה גרירה.** האות "יש עכשיו גרירה" הוא
   **shared value** (`pullEngaged`) על ה-UI thread, אף פעם לא בוליאן של React שיורד
   לדף. קומיט של React שנוחת על מחווה רצה הוא בדיוק מה שגרם לסגירות המגומגמות.
   הרכיב היחיד שממיר את האות חזרה ל-state הוא `PullScrollView` (הוא חייב לשנות
   prop נייטיב), והילדים שלו הם אותם אלמנטים בדיוק — אז React לא מצייר מחדש כלום
   מתחתיו.
3. **כל שחרור ממשיך את האצבע.** גרירה שהתחייבה יוצאת ב-`Easing.out(cubic)`, וחזרה
   למקום היא `withSpring` שנזרע במהירות השחרור. `withTiming` חשוף היה מאיץ מאפס
   ולכן המשטח "נתקע" לרגע לפני שזז. בנוסף הגרירה נפתחת **מהמקום שבו האצבע נמצאת**
   ברגע ההשתלטות, ולא קופצת בכל מרחק הארביטרציה.

**כרום הבית:**
- **המבורגר צף** בפינה העליונה-התחלה (לא זז כשגוררים כרטיס).
- **גרירה אופקית מכל מקום בבית פותחת את התפריט**, עוקבת אצבע בשני הכיוונים.
- **צ'יפ שם/גיל** קבוע בפינה העליונה-סוף; לידו צ'יפ **"סיום צ'אט"** במצב שיחה.
- **לב/צ'אט צף** קבוע בתחתית-סוף.
- **כפתור דיווח** בתחתית-התחלה של התמונה האחרונה — במכוון קטן ומעומעם, לא תאום של הלב.
- **ביו כאריח בז'** בתחתית התמונה השנייה. בתצוגת הפרופיל של עצמי — אותו אריח בדיוק,
  עם עריכה בתוכו (הכרטיס העצמי זהה פיקסלית לכרטיס התאמה).
- **טיימרים תמיד בתוך כרטיס המצב**, אף פעם לא בכרום.
- **אין בבית**: מונה קרדיטים, מונה צופים, בקרת נראות, ברודקאסט — הכול בהגדרות בלבד.

**כפתור חזור (Android):** דיאלוג → שכבה עליונה → הזמנה נכנסת → יציאה.

### 9.2 כללי פריסה נוספים

- **שוליים תחתונים: אזור הבטיחות בולע את המרווח, לא מתווסף אליו.** יש פונקציה אחת
  (`bottomGap(safeInset, gap)` = `Math.max`) וכל משטח קורא לה. לפני כן ה-34px של
  אינדיקטור הבית באייפון נערמו על המרווח של המשטח (50px) והתוכן נדחף מעל פס ריק
  שאין לו מקבילה בשום אפליקציה. אנדרואיד מדווח 0, ולכן הבאג היה גלוי רק באייפון.
- **כיוון הקריאה מוצהר פעם אחת** (`TEXT_START` ב-`fonts.ts`, מוחל ב-`AppText`).
  ב-iOS טקסט בלי יישור מקבל "natural", ו-iOS פותר את זה מול שפת ה-UI של הבילד
  (שאין בו לוקליזציה עברית) — כך שכל תווית לא-מיושרת נדחפה לקצה ההפוך במסך RTL.
  `writingDirection` הוא prop של iOS בלבד, אז לאנדרואיד אין לזה שום השפעה.
  **`textAlign:'right'` הוא המלכודת** — הוא אומר "קצה סוף הכתיבה", כלומר פיזית שמאל ב-RTL.
- **שני Modal לא נפתחים באותו טיק.** גיליון שפותח דיאלוג אחריו חייב לשמור את הכוונה
  ולירות אותה מ-`onClosed` — אחרת הדיאלוג פשוט לא נפתח והתפריט גם ננעל (זה היה הבאג
  של "סיום שיחה"/"חסימה" מתפריט הצ'אט).
- **גיליון פעולות** (שורת אייקון+תווית בתוך BottomSheet) הוא קומפוננטה אחת
  (`SheetActionRow`) שמשמשת גם את תפריט התמונות וגם את תפריט ההודעה בצ'אט.

**עיצוב:** צבעים שטוחים בלבד (בז' + סגול). **אין גרדיאנטים, אין זגוגית.**
טקסט בפופאפים בסגול מלא. **אין מקף ארוך (—) באף מחרוזת i18n.**

**DRY מוחלט:** כל ערך (צבע/מרווח/משך/z-index/מפתן מחווה/מפתח אחסון) מוגדר **פעם אחת**
כטוקן מרכזי. כל אלמנט UI קיים פעם אחת כקומפוננטה עם props. אין ליטרלים באתר הקריאה.

---

## 10. תפריט והגדרות

עריכת תמונות (2–6) · עריכת ביו · **תצוגה מקדימה של הפרופיל** ·
**משפחה/ילדים** (יש/אין, מספר וגילים, "רוצה ילדים משלי", לוח זמנים) — משפיע על המיון ·
טווח גילים · מין מועדף · טווח מרחק · **מיקום** (מכשיר / בית / עבודה עם השלמת כתובת) ·
**נראות** (גלוי/מוסתר + מונה צופים) · **קרדיטים** (טבלת שתי הבריכות, §5) ·
**מעגלים** (חברים + קבוצות + בקשות) · **הזמנת חבר** (שיתוף `/i/<CODE>` + מונה מצטרפים) ·
שפה · **דיווח באג** (טקסט + תמונה) · פרטי חשבון · מסמכים משפטיים (תקנון/פרטיות/בטיחות ילדים) ·
יציאה · **מחיקת חשבון**.

יחידות מרחק נגזרות מאזור המכשיר (ארה"ב = מיילים) — אין הגדרה באפליקציה.

---

## 11. האתר והאדמין (`web/`)

**דף נחיתה** (`once-lake.vercel.app`), עברית + אנגלית (`?lang=`), פלטת צבעים משותפת
עם האפליקציה דרך טוקנים מיוצרים. המבנה:

1. **הירו**.
2. **שאלה אחת** — הבלוק היחיד בעמוד שפונה לקורא במקום לתאר את המוצר
   ("מתי בפעם האחרונה מישהו שם עליכם את כל הפוקוס?"), ולכן הוא מקבל את הטיפול היחיד
   שאין לאף בלוק אחר: **לוח סגול מלא** (הצבעים הפוכים) עם איור לצידו.
3. **ארבעה מסרים** ותו לא, כל אחד עם איור מסך אפליקציה בצד מתחלף:
   אדם אחד בכל פעם (ההדגשה: **שיחה אחת פתוחה**, לא רק כרטיס אחד על המסך) ·
   ההזמנה חיה 10 דקות · **מעגלים** (אנשים מהעולם שלכם) · התאמה להורים לפי ימים פנויים
   וסופי שבוע, כשהלו"ז לא מוצג לאף אחד.
4. **CTA** לדף ההורדה, עם כפתור צף שנשאר קריא גם מעל הלוח הסגול.

דפי הקישורים `/i/`, `/f/`, `/g/` מנסים לפתוח את האפליקציה ואחרי ~1.2 שנ' נופלים ל-`/download`
עם הפרמטר שמור.

**פאנל אדמין** (Next.js, מאחורי התחברות): מסכי **users** (איתור, קביעת קרדיטים,
`release_page1/2`, reset, מחיקה, הפיכת `is_test`, פעולות קבוצתיות), **groups**,
**reports** (דיווחי משתמשים), **bugs** (דיווחי באג).

---

## 12. מחיצת הבדיקות (`is_test`) — קריטי להבנה

אין staging. **דגל בוליאני אחד** מפריד בין עולם הבדיקות לעולם האמיתי:

```sql
AND other.is_test = COALESCE((me).is_test, false)   -- בתוך others()
```

משתמש בדיקה מותאם **רק** למשתמשי בדיקה, ומשתמש אמיתי **רק** לאמיתיים.
המיגרציה שהקימה את זה **מאמתת את עצמה**: היא בודקת שהעוגן קיים בגוף החי של `others()`
ונכשלת במפורש אם הוא נעלם.

**ב-2026-07-27 הורחבה המחיצה** לשמונה מסלולים שלא עברו דרך `others()` (מעגלים, חברים,
דיווח), כל אחד דוחה פעולה חוצת-מחיצות בקוד שגיאה שהיה כבר חלק מהחוזה שלו:
`app_search_people` / `app_search_groups` (מסוננים) · `app_redeem_invite` (`invite_invalid`) ·
`app_friend_request` (`bad_target`) · `app_friend_respond` (`bad_request`) ·
`app_friend_link_by_code` (`bad_code`) · `app_respond_join` (`no_request`) ·
`app_report` (`bad_target`).

> **`app_report` א-סימטרי במכוון:** בדיקה→אמיתי נדחה, אבל **אמיתי→בדיקה מותר** —
> אם חשבון בדיקה בכל זאת ידלוף, המשתמש האמיתי חייב להישאר מסוגל לדווח עליו.
> בטיחות לא מותנית בשיקולי QA.

**צד הקבוצה נקבע ב-`groups.is_test`** (נחתם מהיוצר), לא מהבעלים בזמן אמת.

> **תקלה שנסגרה באותו יום:** קבוצות דמו שהוזרעו ישירות ל-`public.groups` (בלי לעבור
> ב-`app_create_group`) קיבלו `is_test = false` בדיפולט, בעוד כל הבעלים והחברים שלהן
> חשבונות בדיקה. התוצאה: אישור בקשת הצטרפות החזיר `no_request` לנצח, והקבוצות לא נראו
> בחיפוש ולא נפדו בקוד הזמנה. התיקון בשתי שכבות: (1) הטבעה מחדש של השורות החורגות לפי
> הבעלים, (2) **טריגר `BEFORE INSERT` על הטבלה** שחותם את הדגל מהבעלים — כך שאף כותב
> אחר (זריעה, תיקון אדמין, RPC עתידי) לא יוכל שוב ליפול לצד הלא נכון של המחיצה.
> חמש קבוצות ישנות בלי בעלים נשארו כפי שהיו — אין ממי לגזור, וניחוש הוא בדיוק מה
> שהעמודה נועדה למנוע.

**שני פריטים נשארו בנוהל בלבד:** דיווח באג (מגיע לאותו תור אדמין — הכלל הוא תחילית `QA:`)
ופעולות אדמין בווב (פועלות על כל שורה שנבחרה — הכלל הוא סינון ל"בדיקה" קודם).

---

## 13. תוכנית ה-QA

קיים מסמך נפרד — `QA_TEST_PLAN.md` — **336 מקרי בדיקה, מתוכם 102 מסומנים P0**,
ב-18 סקציות: ENV (הכנת סביבה) · ENT (דרכי הגעה) · AUTH · ONB · GATE · HOME · FIND ·
INV · EXP · CHAT · END · COMM · SET · PUSH · RT · SRV · NFR · E2E.

**קריטריון שחרור:** 100% מה-P0 עוברים · אפס כתיבות מול חשבון שאינו `is_test` ·
אין קריסה במסלול P0/P1 · אינווריאנט הלוג נשמר · אין קרדיט "נעלם" בשום מסלול.

---

## 14. מצב נוכחי ופריטים פתוחים

- **Android בלבד** בייצור (Play track `production`). iOS מוכן בקוד, לא מפורסם.
- **קריסת עלייה ידועה** (Android New Arch): `addViewAt: failed to insert view ...` —
  מרוץ mount ב-Fabric, לא קשור לעבודת המגירה. עוקף כרגע ע"י עטיפת ה-Stack הראשי.
- **רכישות בכסף לא פעילות** — 3/10/50 מסומנות "בקרוב". הכלכלה כולה מבוססת מכסה יומית + הזמנות חברים.
- **שערים גאוגרפיים הוסרו** — נשאר רק שער הנוכחות/פוש.
- **מה שעדיין בעץ העבודה ולא נדחף** (נכון לכתיבת המסמך): כל שכבת ה-UI של העדכון הזה
  (צ'אט, מעגלים, קרדיטים, גרירה, כיוון טקסט), המודולים החדשים `chatCache.ts` /
  `clipboard.ts`, והמיגרציה `20260727230000_group_is_test_stamp.sql`.
  קומיטי האתר (המסר הרביעי, בלוק השאלה, המינוח "מעגלים") **כן** נדחפו.
- `user_groups.hidden` נכנס כתוספת טהורה: השרת כבר מכבד אותו, אבל רק הבילד החדש
  יודע להדליק אותו — עד שהוא יופץ, העמודה למעשה רדומה בשדה.

---

## 15. תמצית להטמעה מהירה

אם צריך לזכור חמישה דברים בלבד:

1. **אדם אחד בכל רגע.** אין רשימות, אין צבירה. הזמנה חיה 10 דקות ונועלת את שני הצדדים.
2. **הכול ב-`users.relations`** — שתי מכונות מצבים (`page1` יוזם, `page2` מקבל) + ארנק + זמינות.
3. **מסך אחד + שכבות.** אין ניווט. כל דבר נסגר בגרירה למטה, במימוש יחיד —
   והגלילה הפנימית תמיד מנצחת את הגרירה.
4. **השרת אטומי ומהיר** — טרנזיציה = RPC אחד; פוש ולוגים תמיד מחוץ למסלול הקריטי;
   שורת לוג אחת לכל בקשה.
5. **אין staging** — `is_test` (על המשתמש **ועל הקבוצה**) הוא כל ההפרדה בין בדיקות
   לייצור, ולכן הוא P0 בכל מסלול.

---

## 16. מה השתנה מאז הגרסה הקודמת של המסמך

מסמך זה עודכן ב-2026-07-27 אחרי גל שינויים שלם. תמצית, לפי נושא:

**מוצר ותוכן**
- "קהילות" → **"מעגלים"** בכל טקסט למשתמש (אפליקציה + אתר). מפתחות הקוד לא השתנו.
- דף הנחיתה קיבל **בלוק שאלה אחד** על לוח סגול הפוך בין ההירו למסרים; המסר הראשון
  מדבר על **שיחה אחת פתוחה** ולא רק על כרטיס אחד; המסר על ההורים מדבר על ימים פנויים
  **וסופי שבוע** ומדגיש שהלו"ז לא נראה לאיש; ירדו הצ'יפים "ציבורית או פרטית" ו"מסמנים פעם אחת".

**מעגלים**
- **חברות מוסתרת בקבוצה** (`user_groups.hidden`) — לנהל בלי לשחק, הדרה סימטרית,
  שחרור מיידי של זוגות לא מחייבים, עם `app_set_group_hidden` (§6.3).
- **תזכורת אחת לצוות** על בקשת הצטרפות בת 48 שעות, פוש `group_pending` (§6.4).
- ניווט: **X בכל דף** (לא חץ חזרה), דף פרופיל במלוא המסך, שם קבוצה במלואו בלי חיתוך,
  שורת מטא שנשברת יפה, ושני **כפתורים חסרי מילים** (גלגל שיניים / עין) בדף קבוצה שאני מנהל.

**צ'אט**
- **לחיצה ארוכה על בועה** → גיליון עם "תגובה להודעה" / "העתקת הטקסט".
- **שלוש הנקודות של "מקליד/ה"** יצאו מהרשימה והפכו לכרום צף ליד ה-X — הן כבר לא
  יכולות להזיז בועה.
- **מטמון התמליל**: כתיבה משוטחת, שטיפה בכניסה לרקע, לא דורס מסך קיים,
  ונמחק כשהשיחה נגמרת ובכל יציאה מהחשבון.
- כפתור מיקרופון/שליחה עובד ממקור אמת אחד (הטקסט בשדה) — נעלמה הדריפט שהשאירה מיקרופון
  על הודעה מוכנה.

**קרדיטים**
- שורת הקרדיטים בהגדרות הפכה ל**טבלת שתי בריכות** (יומי + אקסטרה), עם שעת החידוש
  בתוך שורת היומי, ועם הזמנה לפעולה כשאין אקסטרה (§5).

**ביצועים ותשתית קליינט**
- **עלייה מהדיסק**: הפרופיל נשמר מקומית ונצבע בעלייה במקביל לרשת (§4.2).
- **גרירה**: הגלילה הפנימית תמיד מנצחת · אין רינדור מחדש בגלל גרירה (shared value) ·
  כל שחרור ממשיך את האצבע (ease-out / spring עם מהירות) — §9.1.
- **`bottomGap`**: אזור הבטיחות בולע את המרווח התחתון במקום להיערם עליו (§9.2).
- **כיוון קריאה מוצהר פעם אחת** — תיקון יישור טקסט ב-iOS RTL (§9.2).
- שני Modal לא נפתחים באותו טיק (התיקון ל"סיום שיחה"/"חסימה" מתפריט הצ'אט).

**שרת**
- טריגר `BEFORE INSERT` שחותם `groups.is_test` מהבעלים + הטבעה מחדש של הקבוצות
  שנזרעו לצד הלא נכון (§12).
