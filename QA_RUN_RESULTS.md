# Once — תוצאות הרצת QA

הרצה: 2026-07-27 · מול **הייצור החי** (אותו פרויקט Supabase, edge function אמיתי, אפליקציה על אמולטור)
תוכנית הבדיקות: [QA_TEST_PLAN.md](QA_TEST_PLAN.md)

---

## 1. תמצית

| שכבה | הורצו | עברו | נכשלו | הערה |
|---|---|---|---|---|
| **שרת / HTTP (Stage A)** | 104 | 103 | 0 | ENV, SRV, GATE, FIND, INV, EXP, SET, AUTH · 1 SKIP |
| **שרת / HTTP (Stage B)** | 121 | 121 | 0 | CHAT, END, COMM (קבוצות + חברים), ONB, מחיקת חשבון |
| **שכבת הווב** | 13 | 12 | 1 | דפי נחיתה, הורדה, קישורי הזמנה, דפים משפטיים |
| **UI על אמולטור** | 24 | 24 | 0 | אימות חוזי הפריסה + מסלול המשחק המלא מקצה לקצה |
| **סה"כ** | **262** | **260** | **1** | 1 SKIP · **ממצא אמיתי אחד** |

**כשלי מוצר: 1.** כל שאר הכשלים בהרצה הראשונה (24 ב-Stage A, 2 ב-Stage B) היו **באגים בהארנס
הבדיקה שלי**, אומתו כאלה אחד-אחד מול הקוד החי, ותוקנו לפני שנקבע ממצא. הפירוט בסעיף 5,
כי הוא עצמו לימד דברים על החוזה.

---

## 2. איך זה הורץ (כלים)

- **JWT אמיתי לכל חשבון בדיקה**, בלי לגעת בסיסמאות: `POST /auth/v1/admin/generate_link`
  (`type: magiclink`) → `email_otp` → `POST /auth/v1/verify` → `access_token`. `generate_link`
  **אינו שולח מייל**, רק מייצר את הטוקן.
- **קריאות אמיתיות** ל-`POST /functions/v1/app/<action>` עם אותם headers שהקליינט שולח
  (`apikey` + `Authorization: Bearer`), כלומר כל הנתיב נבדק: auth → dispatcher → RPC → `log`.
- **בדיקת מצב** דרך PostgREST כ-service_role (relations, credits, restrictions, chat, groups, log).
- **הכנת מצב** דרך ה-RPC-ים הקיימים (`app_admin_release_page1/2`, `app_admin_set_credits`) ודרך
  PATCH ממוקד ל-`relations` כדי לזייף `expires_at` לעבר.
- **אמולטור** (`emulator-5554`, Android 16, en-US): adb לצילומי מסך, tap/swipe, דיפ-לינקים.
  האפליקציה נטענה מ-Metro מקומי (פורט 8082) כדי שתריץ את קוד ה-HEAD.
- **התחברות באפליקציה בלי OAuth**: הזרקת סשן דרך `once://login-callback#access_token=…&refresh_token=…`
  (`consumeMagicLinkUrl` → `setSession`). עובד מיידית וזו הדרך להריץ UI על כל חשבון בדיקה.

הסקריפטים: `scratchpad/qa/{lib.mjs, stage-a.mjs, stage-b.mjs, ui.sh}`, פלט ב-`results-a.tsv` / `results-b.tsv`,
צילומי מסך ב-`scratchpad/qa/shots/`.

---

## 3. ✅ מה אומת — עיקרי הדברים

### 3.1 המחיצה בין בדיקות לייצור (ENV, SRV-15/16)
- פול המועמדים של חשבון בדיקה הוא **100% חשבונות בדיקה**; ההפוך גם כן.
- **שמונת השומרים החדשים עובדים דרך HTTP אמיתי**: `redeem_invite` של קבוצה אמיתית →
  `invite_invalid`; `friend_request` למשתמש אמיתי → `bad_target`; `friend_link` בקוד אמיתי →
  `bad_code`; `report` על משתמש אמיתי → `bad_target`, **ואומת שלא נכתבה שום שורה** (0 רשומות דיווח).
- חיפוש אנשים מחזיר רק את הצד של הקורא. חיפוש שם שקיים בשתי המחיצות ("ארז") מחזיר לאמיתי
  את האמיתי בלבד.
- **מסלולים באותה מחיצה לא נפגעו**: `redeem_invite` מחזיר `join_status`, חיפוש מוצא, חברות עובדת.
- ארבעה גלאי דליפה (חברות / חברות-בקבוצה / הגבלות / דיווחים) חזרו **0 שורות** בתחילת ההרצה ובסופה.
- קבוצה חדשה נחתמת אוטומטית ב-`is_test` של היוצר (SRV-17 ✅).

### 3.2 כלכלת הקרדיטים — כל הכללים העדינים
| כלל | תוצאה |
|---|---|
| הזמנה עולה קרדיט אחד ומחזיקה אותו (`held: 1`) | ✅ |
| הטיימר הוא **10 דקות** בדיוק, ושני הצדדים חולקים `expires_at` זהה | ✅ |
| **ביטול מאבד** את הקרדיט המוחזק | ✅ |
| **דחייה מחזירה** אותו למזמין | ✅ |
| **פג תוקף מחזיר** אותו, בשני הכיוונים | ✅ |
| **אישור צורך** אותו (לא מוחזר) + עולה קרדיט למאשר | ✅ |
| החזר **לא חורג** מהתקרה היומית (1) | ✅ |
| ארנק ריק לא יכול להזמין → `no_credits` | ✅ |

### 3.3 חלונות ההגבלה
דילוג → `ignore`, ביטול → `cancel`, דחייה → `decline`, עזיבה → `leave`, דיווח → `block` —
כולם נכתבו, וההדדיות אומתה: אחרי עזיבה הפרטנר יוצא מהפול, ואחרי חסימה הוא לא חוזר.

### 3.4 שערים
- **שער נוכחות**: `notif_perm: denied` → `availability = {state: unavailable, reason: push}`,
  ו-`find` מוחזר **403 `unavailable`**. אישור התראות משחרר מיד.
- **פעולות פירוק אינן חסומות** בזמן שער (`clear1` עבר 200) — בדיוק כפי שהחוזה דורש.
- **שער פרופיל**: חשבון עם חשבון בלבד (בלי תמונות) יכול `find` אבל `invite`/`add` מוחזרים
  **403 `profile_incomplete`**, והוא לא מקבל צופה. ביו לבד לא פותח את השער.
- **חלון נוכחות 24 שעות**: `last_seen` לפני יומיים → נעלם מהפול של כולם; פתיחת האפליקציה
  מחזירה אותו מיד.

### 3.5 צ'אט — ולידציית תוכן
טקסט (כולל trim), תמונה, אודיו, מיקום, לוח זמנים, ותשובה-להודעה. אומת:
- מפתח סטורג' של **משתמש אחר** → 403 (`invalid_image_key` / `invalid_audio_key`).
- לוח זמנים ממי שאין לו ילדים → 403 `schedule_not_allowed`; לוח ריק או חורג → 400.
- **ציטוט שמיוחס לצד שלישי מנוקה**; ציטוט תקין נשמר; תצוגה מקדימה נחתכת ל-140; סוג לא מוכר מנוקה.
- `created_at` שהקליינט שולח מכובד (מפתח הדדופ), אבל `created_at` שעה בעתיד **לא נאמן**.
- גל אודיו לא תקין (3 עמודות במקום 60) לא נשמר, וההודעה עדיין נשלחת.
- הודעה בלי פרטנר → 400 `no_partner`.

### 3.6 קבוצות וחברים
מחזור חיים מלא: יצירה (עם ולידציית שם 60 / תיאור 300), הצטרפות עם אישור → `pending` ללא שורת
חברות, קריאת בקשות ע"י בעלים, ביטול בקשה (אידמפוטנטי), אישור → חברות, בקשה שנצרכה לא ניתנת
לאישור שוב, מינוי מנהל (רק בעלים; לא את הבעלים; לא לא-חבר), אי-אפשר להסיר בעלים, קבוצה פרטית
נעלמת מחיפוש, עזיבה מבטלת גם את הרשאת הניהול (cascade), מחיקה רק לבעלים והקוד מפסיק לעבוד.
חברים: בקשה → אישור/דחייה → הסרה, בקשה כפולה לא שוברת, בקשה לעצמי נדחית, בקשה שנצרכה לא
ניתנת לענות שוב.

### 3.7 חשבון
יצירה (`app/account` הוא הרגע שבו נולדת השורה — נקודת הסימון של ENV-10), תאריך לידה לא חוקי
נדחה, **חשבון חדש נולד `is_test = false`** (ולכן חייב סימון מיידי), יציאה מסמנת
`page2 = {locked, message: logout}` + מנקה טוקן פוש ומיקום, וההתחברות הבאה מחזירה ל-`free`.
מחיקת חשבון מוחקת את השורה.

### 3.8 UI על האמולטור — חוזה הפריסה
מסלול מלא בלחיצות אצבע אמיתיות: בית → לב → פופאפ הזמנה → שליחה → המתנה → (אישור מהצד השני
דרך HTTP) → צ'אט → הודעה נכנסת חיה → תפריט. אומת מהצילומים:
- המבורגר צף בעליון-**התחלה**; צ'יפ שם/גיל בעליון-**סוף**, מודגש; לב צף בתחתון-**סוף**.
- כל הצ'יפים על התמונה הם **אריחי בז' עם הצללה** וסדרם: מרחק/זמן → ילדים → קבוצה משותפת.
- **מרחק במיילים** באמולטור en-US — יחידות נגזרות מהלוקאל, לא מהגדרה.
- פופאפ ההזמנה: כותרת מגונדרת נכון ("Invite **her**"), הטקסט אומר **10 דקות** (תואם למה שנמדד
  בשרת), ותג עלות **1** על הכפתור הסגול.
- **לחיצת האצבע ייצרה בשרת בדיוק את המצב הנכון**: `waiting`, `held: 1`, ואצל הנמענת `pending`
  עם אותו `expires_at`.
- הצ'אט: **אין סרגל כותרת**, רק X צף בעליון-התחלה, הרשימה מתחילה בקצה המסך ונכנסת מתחת לשורת
  המצב (`topInset = 0`) — הדירקטיבה מ-26.7 מיושמת.
- הודעה נכנסת הופיעה **חיה** בצ'אט הפתוח, עם מפריד "Today" ושעה.
- התפריט נפתח בגרירה מהשל, ומציג: מין מועדף, טווח גילים, טווח מרחק, מיקום, נראות,
  **"Credits (1/1 + 3 extra) · Renews tomorrow at 17:00"** — תואם בדיוק לארנק בשרת — חשבון,
  קהילות, דיווח באג. אין מונה קרדיטים/צופים/ברודקאסט במסך הבית.
- מסך ההתחברות: Google + מייל בלבד, **אין כפתור Apple באנדרואיד**, צבעים שטוחים.

---

## 4. ❗ ממצא אמיתי

### F-1 · קישור הזמנה פגום מחזיר 404 במקום את דף ההורדה [חמורה: נמוכה]

**מה קורה.** `web/src/proxy.ts` מנתב ל-`group.html` / `friend.html` רק בהתאמה **קשיחה**
(`/g/\d{6}`, `/f/[A-Za-z0-9]{4,16}`). כל קישור שמפספס בקצת נופל דרך ה-proxy אל אפליקציית
Next ומקבל **404**:

```
/g/12   → 404      /f/@@  → 404
/g/     → 404      /f/    → 404
```

**למה זה באג ולא התנהגות מכוונת.** `web/public/invite.js` מכיל fallback מפורש בדיוק למקרה הזה:

```js
/* Not an invite URL at all (a truncated or hand-edited link): there is
   nothing to hand off, so just go where every stray visitor belongs. */
if (!shape) { location.replace(DOWNLOAD); return; }
```

ה-branch הזה **לא יכול לרוץ אף פעם**, כי הדף לא מוגש עבור נתיב שלא התאים. כלומר הכוונה
המתועדת בקוד ("degraded, never broken") לא מתקיימת: מי שקיבל קישור חתוך בוואטסאפ, או הקליד
אותו ידנית עם טעות, רואה 404 במקום להגיע לדף ההורדה עם ההזמנה.

**תיקון מוצע.** להוסיף ב-proxy תפיסה רחבה ל-`/g/*` ו-`/f/*` שמנתבת לאותו דף hand-off (ואז
ה-fallback ב-`invite.js` עושה את שלו), או להפנות ישירות ל-`/download`.

**מקרה בדיקה קיים:** ENT-22 — כרגע נכשל.

---

## 5. באגים בהארנס שנמצאו ותוקנו (ולמה זה חשוב)

בהרצה הראשונה 24 מקרים נכשלו. **אף אחד לא היה באג במוצר** — וזה תועד כאן כי כל אחד לימד משהו
על החוזה שהתוכנית צריכה לדעת:

| מה שנראה כמו באג | האמת |
|---|---|
| "קוד השגיאה `undefined`" ב-8 מקרים | תשובת שגיאה של האפליקציה היא **מחרוזת JSON חשופה** (`"bad_count"`), לא `{error: …}`. ההארנס חיפש שדה שלא קיים. |
| "אין שורות `log`" (SRV-01) | לטבלת `log` אין עמודת `data` — היא `(created_at, user_id, id, key, status, run_ms, log, user)`. השאילתה שלי נכשלה בשקט. |
| "ההזמנה השנייה במיגזע הצליחה" | הכנת המצב שלי **דרסה את page2 של המוזמנת** ובכך מחקה את ההזמנה הראשונה — כלומר הרסתי את התנאי המקדים. אחרי תיקון: השני מקבל `locked` עם `message: invite` — בדיוק מסלול ה-invite-fail. |
| "קרדיטים לא הוחזרו / לא הוחזקו" | `app_admin_set_credits` הוא `(p_user_id, p_balance, p_extra)` ואני העברתי `p_amount`. כל הגדרות הארנק נכשלו בשקט וגררו 7 כשלים. |
| "`relations.match` חסר אחרי אישור" | `match` **מסונתז בקליינט** (`userStore.ts:250`, מ-`page1.profile`) ואינו שדה שרת. |
| "`data.units` נשמר" | הערך שנשלח (`imperial`) **נזרק** כמצופה; מה שראיתי היה ערך legacy קודם (`metric`) על השורה. |

**המסקנה המעשית:** התוכנית ב-`QA_TEST_PLAN.md` צריכה לתעד את צורת תשובת השגיאה (מחרוזת, לא
אובייקט) ואת החתימות של ה-RPC-ים העזר, אחרת כל בודק ייפול באותן מלכודות.

---

## 6. מה לא הורץ, ולמה

| נושא | סיבה |
|---|---|
| **התקנה אמיתית מ-Play + Install Referrer** (ENT-02, 07–14, 17, 25) | את ספריית Play Install Referrer אי אפשר לזייף מ-adb. דורש התקנה אמיתית מהחנות דרך קישור אמיתי, או internal-testing track. |
| **תצוגת פוש אמיתית על המכשיר** (PUSH-03/05/06/10) | לא נבדק בהרצה הזו. השליחה עצמה רצה בשרת מאחורי `waitUntil` ואומתה לוגית, אבל הבאנר/המגש לא. |
| **iOS** | מחוץ להיקף (אנדרואיד-only). |
| **RTL על מכשיר בעברית** (NFR-04) | האמולטור השני (`Hebrew_Big`) עלה, אבל כיביתי אותו בזמן שאובחן כשל תהליך; האבחנה התבררה כשגויה (היו 20GB פנויים) והכשל היה חד-פעמי. ניתן להריץ בנפרד. |
| מחוות עדינות (מפתני סוויפ, אנימציות, מקלדת) | דורש עין אנושית או כלי מדידה; adb מאמת "נפתח/נסגר", לא "נראה חלק". |

---

## 7. מה נשאר במצב שונה אחרי ההרצה

- חשבונות הבדיקה (`Roy`, `Rotem`, `Yael`, `Tal`, `Adi`) עברו מחזורי הזמנה/צ'אט/עזיבה, ולכן
  **יש ביניהם הגבלות** (`leave` 14 יום, `block` מדיווח). לניקוי: מחיקת השורות מ-`restrictions`.
- החשבון שמחובר לאמולטור (`ofir.aviram@gmail.com`, `is_test = true`) **הוחזר למצב נקי**:
  `page1: free`, `page2: free`, ארנק `1 + 3 extra`, בלי הגבלות מול Yael, ושורות הצ'אט של
  ה-QA נמחקו.
- קבוצת ה-QA שנוצרה נמחקה בסוף התרחיש; משתמש ה-onboarding הזמני נמחק (גם השורה, גם ה-auth).
- דיווחי ה-QA (`note` שמתחיל ב-`QA`) נמחקו כדי לא לזהם את תור המודרציה.
- **Metro שרץ על פורט 8081 נמצא במצב שבור** (`runtimeversion:resolve` נכשל שם) — לא נגעתי בו;
  ההרצה השתמשה ב-Metro חדש על 8082. כדאי להרוג את התהליך הישן.
