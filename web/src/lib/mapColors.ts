// The three map accent hexes, defined once. The areas overview circles and
// the live-users presence rings both draw from these so "active/live" and
// "disabled/idle" can never visually drift apart. (Same emerald/amber/zinc
// the StatusBadge uses via Tailwind classes.)
export const MAP_EMERALD = "#10b981";
export const MAP_AMBER = "#f59e0b";
export const MAP_ZINC = "#71717a";
