# Once — תוכנית בדיקות QA מלאה

גרסת מסמך: 2026-07-26 · נכתב מול הקוד ב-`HEAD` ומול פונקציות ה-DB החיות.

---

## 0. מבוא, סביבות וכלי עזר

### 0.1 מטרה והיקף
כיסוי מלא של מסלול המשתמש מהרגע שהוא נחשף לקישור/חנות ועד שיחת צ'אט מלאה וסיומה,
כולל כל מצבי הביניים, מצבי כשל, גזירות זמן (פג תוקף), מצבי מיגזע (race), הרשאות,
קבוצות/חברים, קרדיטים, התראות, ושכבת השרת.

### 0.1.1 כלל ברזל — בדיקות רצות **רק** על משתמשים מסומנים כמשתמשי בדיקה

> **כל מקרה בדיקה במסמך הזה, ללא יוצא מן הכלל, מורץ רק על חשבונות שמסומנים `users.is_test = true`.**
> אין סביבת staging נפרדת: הבדיקות והייצור חולקים אותו פרויקט Supabase ואותו מסד נתונים.
> ההפרדה היחידה בין "עולם הבדיקות" ל"עולם האמיתי" היא הדגל `is_test`, שנאכף
> בסעיף אחד ובלתי-מותנה בתוך `others()` — נקודת החנק היחידה לכל מועמדות:
> ```sql
> AND other.is_test = COALESCE((me).is_test, false)
> ```
> כלומר: משתמש בדיקה מותאם **רק** למשתמשי בדיקה, ומשתמש אמיתי **רק** למשתמשים אמיתיים.

**מה זה אומר בפועל:**
1. לפני תחילת מחזור בדיקות — מסמנים כל חשבון בדיקה (`A`, `B`, `C`, `D`, `OWNER`, `MGR` וכל חשבון חדש שנוצר במהלך הבדיקות) כ-`is_test = true`, **לפני** שהוא משלים פרופיל ולפני כל פעולת משחק.
2. חשבון חדש נולד `is_test = false` (ברירת המחדל של העמודה). לכן כל התקנה טרייה במסלולי ENT מחייבת סימון מיד אחרי יצירת החשבון (בסוף שלב 3 של האונבורדינג) ולפני העלאת התמונות — ראה **ENV-10**.
3. **אסור** לבצע פעולת כתיבה (הזמנה, אישור, צ'אט, דיווח, חסימה, הסרה) מול חשבון אמיתי. חשבון אמיתי משמש לכל היותר כייחוס לקריאה בלבד.
4. אם בדיקה כלשהי מגלה חשבון אמיתי בפול של חשבון בדיקה (או להפך) — **עוצרים את המחזור**. זו תקלה חוסמת (P0), לא הערה.
5. גם התגמולים חוצי-הצדדים כפופים למחיצה: תשלום על הזמנת חבר משולם **רק כששני הצדדים באותה מחיצה**. זוג מעורב (בדיקה + אמיתי) מסומן `skipped` ואינו משלם לאף אחד — ראה **ENV-12**.
6. הדגל אינו קלט לשער הזמינות: משתמש בדיקה הוא משתמש מן המניין מכל בחינה אחרת (הרשאות, קרדיטים, פוש, תפוגה) — ההבדל היחיד הוא מי מותר לו לפגוש.

### 0.1.2 כלל עומד: **כל יצירת חשבון חדש — מסמנים אותו כמשתמש בדיקות מיד**

> **בכל פעם שנוצר חשבון חדש במהלך הבדיקות, מיד לאחר יצירת החשבון מסמנים אותו
> `is_test = true` — לפני כל פעולה נוספת באפליקציה.**
> זה לא צעד של סקציה מסוימת אלא כלל עומד שחל על כל המסמך, בכל פעם שנוצר חשבון,
> ולא משנה איך הגיע: הורדה חופשית מהחנות, קישור הזמנה אישי, קישור חבר, קישור קבוצה,
> התחברות עם Google / Apple / קישור באימייל, חשבון בדיקת חנות, או חשבון שנוצר מחדש
> אחרי מחיקת חשבון.

**מתי בדיוק החשבון "נוצר":** בסוף שלב 3 של האונבורדינג (מין + שם + תאריך לידה) —
זה הרגע שבו הקליינט שולח `app/account` והשורה נכתבת ב-`users`. לפני זה יש סשן אימות
בלבד ואין שורת משתמש לסמן.

**הרצף המחייב:**
1. השלם שלב 3 של האונבורדינג.
2. **עצור מיד** — אל תעלה תמונה, אל תלחץ play, אל תשלח כלום.
3. סמן:
```sql
update users set is_test = true
where user_id in (select id from auth.users where email = '<EMAIL>');
```
4. אמת שהחזיר שורה אחת ושהדגל אכן `t`.
5. רק עכשיו המשך לתמונות, לביו ולשאר התרחיש.

**למה דווקא בחלון הזה:** `others()` מציג משתמש רק אם יש לו לפחות תמונה אחת. חשבון
שסומן לפני התמונה הראשונה מעולם לא היה גלוי לפול האמיתי — אף לא לשנייה אחת.

**אם נשכח לסמן ("נוהל התאוששות"):** 1) סמן מיד. 2) הרץ
`app_admin_release_page1` + `app_admin_release_page2` על החשבון כדי לפרק כל צפייה /
הזמנה / צ'אט שנוצרו בטעות מול משתמש אמיתי (הפונקציות מתקנות גם את הצד השני ומחזירות
קרדיט מוחזק). 3) בדוק אם נוצרו הגבלות מול חשבונות אמיתיים
(`select * from restrictions where user_id='<UID>' or other_id='<UID>'`) ונקה אותן.
4) תעד את האירוע — זו תקלת ייצור, לא הערת בדיקה.

### 0.1.3 מסלולי הקהילות, החברים והדיווח — נחסמו בקוד (2026-07-27)

**רקע.** עד 2026-07-27 הדגל `is_test` נאכף בנקודה אחת בלבד: `others()`, כלומר **המשחק**
(מציאת מועמד, זריעת צופה, ברודקאסט, ומכאן גם הזמנה, אישור וצ'אט — כולם מותנים בקישור
שנוצר דרך המשחק). ביקורת QA מצאה שישה מסלולים שלא עוברים דרך `others()` ולכן אפשרו
לחשבון בדיקה להגיע למשתמש אמיתי, לפעמים עם פוש שאי אפשר לבטל.

**המסלולים האלה נחסמו בשרת** במיגרציה `20260727120000_test_partition_communities.sql`.
כל אחד מהם דוחה כעת פעולה חוצת-מחיצות בקוד שגיאה שהיה **כבר** חלק מהחוזה שלו, כך
שהאפליקציה מציגה אותו כמו כל תנאי מקדים שנדחה, ואין שינוי בצורת התשובה:

| מסלול | התנהגות חדשה | קוד שגיאה |
|---|---|---|
| `app_search_people` | מחזיר רק את הצד של הקורא | (מסונן, לא שגיאה) |
| `app_search_groups` | מחזיר רק קבוצות בצד של הקורא | (מסונן, לא שגיאה) |
| `app_redeem_invite` | קוד של קבוצה בצד השני = כאילו לא קיים | `invite_invalid` |
| `app_friend_request` | יעד בצד השני = כאילו לא קיים | `bad_target` |
| `app_friend_respond` | אישור בקשה חוצת-מחיצות (רשומה עתיקה) נדחה; דחייה עדיין עובדת | `bad_request` |
| `app_friend_link_by_code` | קוד `/f/` של הצד השני נדחה | `bad_code` |
| `app_respond_join` | אישור בקשת הצטרפות חוצת-מחיצות נדחה; דחייה עובדת | `no_request` |
| `app_report` | **א-סימטרי:** בדיקה → אמיתי נדחה; **אמיתי → בדיקה מותר** | `bad_target` |

**למה `app_report` א-סימטרי:** חסימה מדיווח היא לצמיתות ואין לה חלון תפוגה, ודיווח
מחשבון בדיקה על אדם אמיתי הוא תמיד תקלה. אבל אם חשבון בדיקה כן ידלוף אל הפול של
משתמש אמיתי, אותו משתמש **חייב** להישאר מסוגל לדווח עליו. בטיחות אינה מותנית בשיקולי QA.

**חלוקת הקבוצות היא לפי `groups.is_test`, לא לפי הבעלים.** חמש קבוצות ותיקות נושאות
`owner_id = NULL` (אחת מהן עם 37 חברי בדיקה, אחרת עם 20 אמיתיים), ולכן גזירה מהבעלים
הייתה נועלת את 37 חשבונות הבדיקה מחוץ לקבוצה שלהם ובמקביל פותחת להם את האמיתית. הדגל
נחתם בקבוצה בזמן היצירה מהיוצר, בדיוק כמו ב-`users`.

**מה זה משנה לבודק:** ההגנה היום מבנית ולא נוהלית. הכללים למטה **נשארים בתוקף כהגנה
בשכבה שנייה** — הם מונעים מהבודק לבזבז זמן על תוצאות שיידחו, שומרים על נתוני בדיקה
מובחנים באדמין, ומגנים על התרחישים שהמיגרציה במכוון לא חסמה (דיווח באג, פעולות אדמין,
ודיווח בכיוון אמיתי→בדיקה):

1. **שמות חשבונות הבדיקה נושאים תחילית ייחודית** — `QA-A`, `QA-B`, `QA-OWNER` וכו'.
2. **בחיפוש אנשים מחפשים רק את התחילית `QA-`.** אסור לחפש שם גנרי ("דני", "נועה", "א").
   תוצאה שאינה מתחילה ב-`QA-` — לא נוגעים בה, לא שולחים אליה בקשה, לא פותחים אותה.
3. **בחיפוש קבוצות מחפשים רק את התחילית `QA-`.** כל קבוצות הבדיקה נוצרות ע"י `QA-OWNER`
   בשם `QA-<משהו>`. אסור להצטרף לקבוצה שלא נוצרה במחזור הבדיקות הזה.
4. **קודי הזמנה (אישי / חבר / קבוצה) נלקחים תמיד מחשבון בדיקה** — מהאפליקציה של חשבון
   הבדיקה או מה-DB. אסור להמציא קוד, לנחש, או להשתמש בקוד ממקור חיצוני.
5. **בדיקת "קוד לא תקין" מחייבת אימות מוקדם שהקוד לא קיים** (ראה ENT-26): קוד בן 6 ספרות
   שהומצא עלול להתאים לקבוצה אמיתית וההצטרפות תתבצע בפועל.
6. **דיווח וחסימה מבוצעים רק מול חשבון בדיקה.** דיווח על משתמש אמיתי הוא בלתי הפיך:
   הוא יוצר חסימה לצמיתות ורשומה שתגיע לאדמין כאילו הייתה תלונה אמיתית.
7. **באדמין הווב** אין להפעיל `is_test` flip, `release_page1/2`, איפוס או מחיקה על שורה
   שאינה חשבון בדיקה. סינון הסביבה במסך המשתמשים מוגדר ל"בדיקה" **לפני** כל פעולה קבוצתית.
8. **טקסט דיווח באג** (SET-16) מתחיל ב-`QA:` כדי שיהיה מובחן מדיווח אמיתי באדמין.

> אם בטעות נוצרה חברות / הצטרפות לקבוצה / דיווח מול משתמש אמיתי: בטל מיד
> (`app_unfriend`, `app_leave_group`, ומחיקת רשומת ההגבלה והדיווח ב-SQL), ותעד. הפוש
> שנשלח אינו הפיך — לכן הכלל הוא מנע, לא תקן.

### 0.2 עובדות מערכת מאומתות (בסיס לתוצאות המצופות)
| פרמטר | ערך | מקור |
|---|---|---|
| תוקף הזמנה | 10 דקות | `app_invite`: `now() + interval '10 minutes'` |
| מכסת קרדיט יומית | 1 (`_credits_cap`) | מתחדש ב-20:00 Asia/Jerusalem |
| עלות הזמנה / אישור / ברודקאסט | 1 / 1 / 1 (`_credits_cost`) | ביטול = 0 (הקרדיט המוחזק מתאבד) |
| תגמול הזמנת חבר | 1 (`_referral_reward`) | משולם רק כשהמוזמן עובר `>= 1` תמונה |
| חלון נוכחות | 24 שעות (`_presence_ttl`) | `last_seen` ישן מיום = לא מוצג לאף אחד |
| חסימות `ignore`/`cancel`/`remove` | 1 יום | `others()` |
| חסימת `decline` | 7 ימים | `others()` |
| חסימת `leave` | 14 יום | `others()` |
| חסימת `block` | לנצח | `others()` |
| סוויפ פג-תוקף (קרון) | כל דקה `expire-sweep` | + `app_expire_self` מיידי מהקליינט |
| סוויפ תפוגת צפייה | כל שעה `watch-expire-sweep` | |
| שער זמינות | `push_blocked()` בלבד: `perm='denied'` או `dead=true` (כשיש location) | אזורים גאוגרפיים הוסרו |
| בוסטים ברלוונטיות | קבוצה משותפת ×3, חבר ×3, ברודקאסט 30 דק' ×2 | `others()` |
| תמונות | מינימום 2 באונבורדינג, מקסימום 6 | `PhotoEditor` |
| ביו | 20–150 תווים, שורות ריקות נמחקות | `bio.ts` |
| שם | עד 30 תווים · גיל 18–120 | `onboarding.tsx` |
| קוד קבוצה | 6 ספרות · קוד הזמנה אישי 4–16 אלפאנומרי | `groups.ts`, `referral.ts` |
| הפרדת בדיקות | `other.is_test = me.is_test` — משתמשי בדיקה רואים רק זה את זה | `others()` |

### 0.3 סביבות
- **PROD** (החנות, Play track `production`) — בדיקות עשן בלבד אחרי שחרור.
- **Build פנימי** (`eas build --profile preview`) — עיקר הבדיקות.
- **Expo Go** — לא תקף לבדיקות התראות/Install-Referrer (המודול הנייטיבי חסר). מסומן בכל מקרה רלוונטי.
- שרת: פרויקט Supabase יחיד (אין staging) — לכן כל משתמשי הבדיקה חייבים `is_test=true`.

### 0.4 מכשירים
| # | מכשיר | תפקיד |
|---|---|---|
| D1 | Android 13+ | משתמש A (יוזם) |
| D2 | Android 11/12 | משתמש B (נמען) |
| D3 | Android עם RTL/גופן מוגדל | נגישות + RTL |
| D4 | iPhone (iOS 16+) | רק אם נבדק iOS (כרגע אנדרואיד-only) |

### 0.5 משתמשי בדיקה

**כולם `is_test = true`.** ראה כלל הברזל בסעיף 0.1.1.

| משתמש | מין | `is_test` | פרופיל | שימוש |
|---|---|---|---|---|
| A | ז | **true** | מלא (2 תמונות + ביו) | יוזם הזמנות |
| B | נ | **true** | מלא | נמען |
| C | נ | **true** | חשבון בלבד, בלי תמונות/ביו | browse-only |
| D | ז | **true** | מלא, התראות דחויות | בדיקת שער נוכחות |
| OWNER | — | **true** | מלא | בעל קבוצה |
| MGR | — | **true** | מלא | מנהל קבוצה |
| A2 | ז | **true** | מלא | מכשיר שלישי, נדרש למקרי מיגזע (INV-06, INV-21) |
| REF | נ | `false` | קיים בייצור | **ייחוס לקריאה בלבד.** לא נוגעים בו, לא מזמינים, לא מדווחים. משמש רק לאימות ש-`others()` לא מחזיר אותו לחשבון בדיקה (ENV-09) |

> חשבון בדיקה שנוצר במהלך תרחיש התקנה (ENT) מקבל שם שמזהה אותו כבדיקה (למשל `QA-B-0726`),
> כדי שאם הוא בכל זאת ידלוף לפול האמיתי הוא יזוהה מיד.

### 0.6 כלי עזר (SQL/RPC אדמין)
```sql
-- ── חובה לפני כל דבר אחר: סימון משתמש בדיקה ──────────────────────────────
update users set is_test = true where user_id = '<UID>';
-- סימון לפי אימייל (נוח יותר אחרי התחברות טרייה)
update users set is_test = true
where user_id in (select id from auth.users where email = '<EMAIL>');
-- אימות לפני תחילת מחזור: כל חשבונות הבדיקה מסומנים
select user_id, name, is_test, created_at from users
where user_id in ('<A>','<B>','<C>','<D>','<OWNER>','<MGR>','<A2>');
-- ספירת המחיצות (בקרת שפיות)
select is_test, count(*) from users group by is_test;
-- מי מהחשבונות שנוצרו היום עוד לא סומן (מלכודת הדליפה הנפוצה)
select user_id, name, created_at from users
where not is_test and created_at > now() - interval '1 day' order by created_at desc;
-- קביעת ארנק
select app_admin_set_credits('<UID>', 3);
-- שחרור מצב תקוע
select app_admin_release_page1('<UID>');  -- מבטל page1 (cancel/leave)
select app_admin_release_page2('<UID>');  -- מבטל page2 (decline/remove)
select app_admin_reset('<UID>');          -- איפוס מלא
-- זיוף פג תוקף מיידי
update users set relations = jsonb_set(relations,'{page1,expires_at}', to_jsonb((now()-interval '1 minute')::text)) where user_id='<UID>';
-- זיוף נוכחות ישנה (יציאה מהפול)
update users set last_seen = now() - interval '2 days' where user_id='<UID>';
-- בדיקת לוג (חייבת שורה אחת לכל בקשה)
select key, created_at, data->>'error' from log where user_id='<UID>' order by created_at desc limit 20;
-- מועמדים בפועל
select user_id, relevance from others((select u from users u where u.user_id='<UID>'), true) order by relevance desc limit 10;
```

### 0.7 מקרא
`☐` לא הורץ · `✅` עבר · `❌` נכשל · `⚠️` עבר עם הערה · `⛔` חסום
כל מקרה כולל: **קדם** (תנאי מקדים) · **צעדים** · **מצופה** · **אימות שרת** (כשרלוונטי).

### 0.7.1 צ'ק-ליסט טרום-הרצה (חובה בתחילת כל מחזור)
- ☐ כל חשבונות הבדיקה מסומנים `is_test = true` (הרץ את שאילתת האימות בסעיף 0.6).
- ☐ אין חשבון שנוצר ב-24 השעות האחרונות שנשאר `is_test = false` בטעות.
- ☐ ENV-09 עבר: `others()` של חשבון בדיקה מחזיר **רק** חשבונות בדיקה.
- ☐ ההגבלות בין זוגות הבדיקה נוקו (ENV-04).
- ☐ ארנקי הבדיקה בערך ידוע (ENV-05).
- ☐ **כלל עומד מופנם: כל חשבון חדש שייווצר במהלך המחזור מסומן `is_test = true` מיד עם יצירתו** (סעיף 0.1.2).
- ☐ **כל שמות חשבונות הבדיקה נושאים את התחילית `QA-`** (סעיף 0.1.3, כלל 1) — תנאי הכרחי לבדיקות חיפוש אנשים.
- ☐ **קבוצות הבדיקה נוצרו ע"י `QA-OWNER` עם תחילית `QA-`** ולא נעשה שימוש בקבוצה קיימת.
- ☐ ENV-13 עבר: אין חברות / חברות-קבוצה / הגבלה שמקשרת חשבון בדיקה למשתמש אמיתי.
- ☐ ENV-15 עבר: `groups.is_test` תואם לכל קבוצה עם בעלים.
- ☐ הבודק קרא את סעיף 0.1.3 ומכיר את קודי השגיאה שהשומרים מחזירים (`invite_invalid` / `bad_target` / `bad_code`) כדי לא לפתוח עליהם באג.

### 0.8 קריטריון שחרור
- 100% מהמקרים המסומנים **[P0]** עוברים.
- **אפס** פעולות כתיבה שבוצעו מול חשבון שאינו `is_test = true` (סעיף 0.1.1).
- אין קריסה בשום מסלול P0/P1.
- אינווריאנט לוג: לכל בקשת HTTP שורה אחת ב-`log` (SRV-01).
- אין קרדיט "נעלם" בשום מסלול הזמנה (INV/EXP).

---

## 1. ENV — הכנת סביבה ונתוני בדיקה

> סקציה זו אוכפת את כלל הברזל מסעיף 0.1.1. **אין להתחיל אף סקציה אחרת לפני ש-ENV-01, ENV-09 ו-ENV-10 עברו.**

**ENV-01 [P0] · סימון משתמשי בדיקה — שער הכניסה למחזור**
- קדם: כל חשבונות הבדיקה קיימים (לפחות חשבון = שם + תאריך לידה + מין).
- צעדים: 1) הרץ `update users set is_test = true` לכל אחד מהם. 2) הרץ את שאילתת האימות בסעיף 0.6.
- מצופה: כל שורה מחזירה `is_test = t`. אף חשבון בדיקה לא נשאר `false`.
- אימות: `select user_id, is_test from users where user_id in (...)` — אין `f` באף שורה.
- **אם נכשל:** עוצרים. אין להריץ שום מקרה בדיקה אחר עד שכל החשבונות מסומנים.

**ENV-09 [P0] · אימות המחיצה בפועל — חשבון בדיקה לא רואה חשבון אמיתי**
- קדם: ENV-01 עבר; קיים לפחות חשבון אמיתי אחד (REF) שעומד בקריטריוני התאמה (מין/גיל/טווח) מול A.
- צעדים: הרץ את פול המועמדים של A:
```sql
select o.user_id, u.name, u.is_test, o.relevance
from others((select u from users u where u.user_id='<A>'), true) o
join users u on u.user_id = o.user_id;
```
- מצופה: **כל** השורות `is_test = t`. REF לא מופיע. הפוך: הרצת אותה שאילתה עבור REF לא מחזירה אף חשבון בדיקה.
- **אם נכשל:** תקלה חוסמת — סימן שהסעיף `AND other.is_test = COALESCE((me).is_test, false)` נעלם מ-`others()` בעקבות מיגרציה. עוצרים את המחזור ומדווחים מיד.

**ENV-10 [P0] · סימון חשבון שנוצר במהלך תרחיש התקנה**
- הקשר: כל מסלולי ENT (הורדה חופשית, `/i/`, `/f/`, `/g/`) יוצרים חשבון **חדש** שנולד `is_test = false`.
- צעדים: 1) השלם שלב 3 באונבורדינג (שם/תאריך/מין) — כאן החשבון נוצר בשרת. 2) **עצור.** סמן `is_test = true` לפי אימייל. 3) רק אז המשך לתמונות ולביו.
- מצופה: החשבון מסומן לפני שהוא מקבל תמונה, כלומר לפני שהוא הופך גלוי לאחרים (`others()` דורש `images >= 1`) — כך הוא לא מופיע בפול האמיתי אף לא לרגע.
- **אם נשכח:** החשבון היה גלוי לאמיתיים. סמן מיד, ואז הרץ `app_admin_release_page1/2` עליו כדי לפרק כל קישור שנוצר בטעות, ותעד את האירוע.

**ENV-11 · העברת חשבון בין מחיצות מהאדמין**
- צעדים: בממשק האדמין (מסך users, מסנן הסביבה) העבר חשבון מ"אמיתי" ל"בדיקה".
- מצופה: הדגל מתהפך בעדכון אחד; מיד אחריו השרת מפרק את שני העמודים של אותו חשבון (`app_admin_release_page1/2`) — צפייה מתנתקת, הזמנה ממתינה נסגרת, צ'אט מסתיים, צופים מועפים, וקרדיט מוחזק מוחזר. לא נשאר קישור "תלוי" חוצה-מחיצות.
- אימות: `select relations->'page1', relations->'page2' from users where user_id='<UID>'` — נקי.

**ENV-12 [P0] · תגמולים אינם עוברים בין מחיצות**
- קדם: מזמין `is_test=true`, מוזמן `is_test=false` (או להפך).
- צעדים: השלם מסלול הזמנת חבר (`/i/` או `/f/`) מקצה לקצה כולל השלמת פרופיל אצל המוזמן.
- מצופה: **אף צד לא מקבל קרדיט.** ההזמנה מסומנת `skipped` ולא `credited`.
- אימות: `select qualified_at, credited_at from referrals where invitee_id='<UID>'` → `qualified_at` מלא, `credited_at` ריק.
- מסקנה מעשית: **כל בדיקת תגמול (ENT-08, ENT-17, SET-14) מחייבת ששני הצדדים יהיו `is_test=true`.** אחרת הבדיקה תיראה כמו באג בתשלום כשהיא בעצם המחיצה עושה את עבודתה.

**ENV-13 [P0] · גלאי דליפה חוצה-מחיצות (לפני ואחרי כל מחזור)**
- הקשר: מאז המיגרציה של 2026-07-27 השרת חוסם יצירת קישורים חוצי-מחיצות (סעיף 0.1.3), אבל הגלאי נשאר P0 מ**שתי** סיבות: הוא מאתר רשומות שנוצרו לפני החסימה, והוא בדיקת הרגרסיה שתתפוס אם מיגרציה עתידית תסיר שוב אחד השומרים.
- צעדים: הרץ את שלוש השאילתות. **כל אחת חייבת להחזיר 0 שורות.**
```sql
-- 1) חברויות חוצות-מחיצות
select fl.a, fl.b, ua.name as a_name, ua.is_test as a_test, ub.name as b_name, ub.is_test as b_test
from friend_links fl
join users ua on ua.user_id = fl.a
join users ub on ub.user_id = fl.b
where ua.is_test is distinct from ub.is_test;

-- 2) חבר שנמצא בקבוצה מהצד השני של המחיצה.
--    ההשוואה מול groups.is_test (הדגל של הקבוצה עצמה), לא מול הבעלים: חמש
--    קבוצות ותיקות בלי owner_id היו מייצרות התראות שקר בהשוואה לבעלים.
select u.name as member, u.is_test as member_test, g.name as group_name, g.is_test as group_test
from user_groups ug
join users u on u.user_id = ug.user_id
join groups g on g.id = ug.group_id
where u.is_test is distinct from g.is_test;

-- 3) הגבלות (חסימה/דחייה/עזיבה) חוצות-מחיצות — עקבות של דיווח או משחק שלא היה צריך לקרות
select r.key, r.created_at, ua.name, ua.is_test, ub.name, ub.is_test
from restrictions r
join users ua on ua.user_id = r.user_id
join users ub on ub.user_id = r.other_id
where ua.is_test is distinct from ub.is_test
order by r.created_at desc;
```
- מצופה: 0 שורות בשלושתן, גם לפני המחזור וגם בסופו.
- **אם חוזרות שורות:** תקלה חוסמת. נקה לפי סעיף 0.1.3 (בטל חברות, צא מהקבוצה, מחק את ההגבלה), תעד את האירוע, ובדוק איזה מקרה בדיקה יצר אותו — כנראה חיפוש בשם גנרי או קוד שלא נלקח מחשבון בדיקה.

**ENV-14 · תחילית `QA-` בכל שמות הבדיקה**
- צעדים: `select user_id, name, is_test from users where is_test;`
- מצופה: כל שם מתחיל ב-`QA-`. שם בדיקה בלי תחילית שובר את חוקי החיפוש בסעיף 0.1.3 ולכן נחשב תקלת הכנה.
- כנ"ל לקבוצות: `select id, name, is_test from groups where is_test;` — כולן `QA-...`.

**ENV-15 [P0] · דגל המחיצה על קבוצות (`groups.is_test`)**
- הקשר: הדגל נחתם בזמן יצירת הקבוצה מהיוצר. קבוצה בלי דגל נכון מנותקת מהצד שלה: הבעלים לא ימצא אותה בחיפוש והחברים לא יוכלו לממש את הקוד שלה.
- צעדים:
```sql
-- כל קבוצה של יוצר בדיקה חייבת is_test = true, וההפך
select g.name, g.is_test as group_test, o.name as owner, o.is_test as owner_test
from groups g left join users o on o.user_id = g.owner_id
where g.owner_id is not null and g.is_test is distinct from o.is_test;
```
- מצופה: 0 שורות. קבוצה שנוצרה במחזור הזה ע"י `QA-OWNER` חוזרת עם `is_test = t`.
- הערה 1: אחרי העברת משתמש בין מחיצות באדמין, הקבוצות שבבעלותו עוברות איתו אוטומטית (ENV-11) — הרץ את השאילתה הזו כדי לאמת.
- הערה 2: קבוצה שנוצרת **מהאדמין בווב** (מסך הקבוצות) נולדת ללא בעלים ובמחיצה האמיתית (`is_test = false`), כי זו ברירת המחדל של העמודה. קבוצת בדיקה נוצרת מהאפליקציה ע"י `QA-OWNER`, שם הדגל נחתם אוטומטית. אם בכל זאת נוצרה קבוצת בדיקה מהאדמין, סמן אותה ידנית: `update groups set is_test = true where id = '<GID>';`

**ENV-02 · שני מכשירים באזור אחד**
- צעדים: הגדר ל-A ו-B מיקום זהה (או בטווח), טווח חיפוש "ללא הגבלה".
- מצופה: כל אחד מופיע כמועמד אצל השני (בהתחשב בהעדפות מין/גיל).

**ENV-03 · העדפות מין/גיל מצטלבות**
- מצופה: הכלל דו-כיווני — A מחפש נשים **וגם** B מחפשת גברים. אם צד אחד לא מסומן, אין התאמה.

**ENV-04 · ניקוי הגבלות בין A ל-B**
```sql
delete from restrictions where (user_id='<A>' and other_id='<B>') or (user_id='<B>' and other_id='<A>');
```
- מצופה: אחרי ניקוי, השניים חוזרים לפול זה של זה מיד.

**ENV-05 · ארנק מבוקר**
- מצופה: `app_admin_set_credits` מעדכן `relations.credits.balance`, ותצוגת הקרדיטים בתפריט מתעדכנת ב-Realtime בלי רענון.

**ENV-06 · EAS env vars**
- צעדים: בנה preview, פתח את האפליקציה.
- מצופה: אין קריסת `supabaseUrl is required` (כלומר `EXPO_PUBLIC_SUPABASE_URL/ANON_KEY` קיימים בסביבת EAS).

**ENV-07 · לוגים נגישים**
- מצופה: `select * from log order by created_at desc` מציג את הפעולות תוך שניות.

**ENV-08 · Expo Go לעומת build**
- מצופה: ב-Expo Go — Install Referrer לא זמין (בלי שגיאה), התראות לא נרשמות. תרחישי ENT-02..ENT-08 נבדקים רק על build אמיתי.

---

## 2. ENT — דרכי הגעה לאפליקציה (התקנה, קישורים, דיפ-לינקים)

> ⚠️ **כל מקרה בסקציה זו יוצר חשבון חדש.** בכל אחד מהם, מיד עם יצירת החשבון (סוף שלב 3
> של האונבורדינג) מסמנים אותו `is_test = true` ורק אז ממשיכים — ראה סעיף 0.1.2 ו-ENV-10.
> הצעד הזה אינו חוזר במפורש בכל מקרה כדי לא להאריך; הוא חל על כולם.
>
> ⚠️ **כל הקודים בסקציה זו** (קוד הזמנה אישי, קוד חבר, טוקן/קוד קבוצה) נלקחים **רק**
> מחשבון בדיקה או מקבוצה שנוצרה ע"י `QA-OWNER` במחזור הזה. קוד מומצא או ממקור חיצוני
> עלול להתאים למשתמש/קבוצה אמיתיים, ואז ההצטרפות והפוש קורים בפועל (סעיף 0.1.3).

### 2.1 הורדה חופשית (אורגנית)

**ENT-01 [P0] · התקנה אורגנית מ-Play, בלי שום קישור**
- קדם: מכשיר נקי, האפליקציה מעולם לא הותקנה.
- צעדים: 1) התקן מ-Play. 2) פתח. 3) התחבר. 4) השלם אונבורדינג.
- מצופה: מסך התחברות → אונבורדינג → בית. אין שיוך מזמין, אין קבוצה, אין חבר.
- אימות: אין שורה ב-`referrals` עבור המשתמש; `AsyncStorage[referralClaimed]='done'`.

**ENT-02 · Install Referrer אורגני ("utm_medium=organic")**
- מצופה: `claimInstallReferral` מזהה שאין `ref`/`grp`, מסמן `done` ולא מנסה שוב אף פעם. אין קריאת `app/referral` בלוג.

**ENT-03 · התקנה מ-APK (sideload)**
- מצופה: אין Install Referrer, האפליקציה עולה רגיל, בלי שגיאה למשתמש.

**ENT-04 · דף הנחיתה של האתר**
- צעדים: פתח `https://once-lake.vercel.app` בדפדפן מובייל.
- מצופה: ארבעת המסרים + CTA שמוביל ל-`/download`; מ-Android — קישור לחנות.

**ENT-05 · דף `/download` ישירות**
- מצופה: כפתור הורדה עובד; פתיחה עם `?grp=...`/`?ref=...` שומרת את הפרמטר בקישור לחנות (`store.js`).

**ENT-06 · שפה בדפי האתר**
- מצופה: `?lang=he` / `?lang=en` מחליף שפה; ברירת מחדל עברית. אין רוסית.

### 2.2 קישור הזמנה אישי לקרדיט (`/i/<CODE>`)

**ENT-07 [P0] · שיתוף קישור מזמין**
- צעדים: A → תפריט → שורת הזמנת חבר → שיתוף.
- מצופה: תיבת שיתוף עם טקסט + `https://once-lake.vercel.app/i/<CODE>` בשורה נפרדת.
- אימות: `<CODE>` = `users.referral_code` של A.

**ENT-08 [P0] · מוזמן מתקין מהקישור ומשלים פרופיל → A מקבל קרדיט**
- קדם: B מכשיר נקי; ארנק A ידוע; **שני הצדדים `is_test=true`** — סמן את B בסוף שלב 3 של האונבורדינג לפי ENV-10, אחרת התגמול לא ישולם בכלל (ENV-12).
- צעדים: 1) B פותח `/i/<CODE>` → מנותב ל-Play עם `referrer=ref=<CODE>`. 2) מתקין ופותח. 3) מתחבר. 4) **סמן `is_test=true`.** 5) משלים אונבורדינג כולל תמונה.
- מצופה: B לא רואה שום אזכור לקוד באונבורדינג (השיוך שקוף). A מקבל +1 קרדיט ופוש `referral` עם שם B בכותרת; מונה "הצטרפו" של A עולה ב-1.
- אימות: שורה ב-`referrals` עם `invitee_id=B`; `relations.referral.joined` של A עלה.

**ENT-09 · מוזמן מתקין אך לא מעלה תמונה**
- מצופה: אין תשלום למזמין. השיוך רשום כ"ממתין".
- אימות: השורה ב-`referrals` קיימת אבל לא שולמה; אחרי העלאת תמונה ראשונה (`app/profile`) — משולם (או ע"י `app_referral_sweep` הדקתי).

**ENT-10 · שיוך עצמי**
- צעדים: A מכניס את הקוד של עצמו (דרך API).
- מצופה: `self_referral` 400, בלוג בלבד, המשתמש לא רואה שגיאה בזרימה השקטה.

**ENT-11 · שיוך כפול**
- מצופה: קריאה שנייה מחזירה `already`, לא שגיאה, ולא משלמת פעמיים.

**ENT-12 · חשבון ותיק מדי**
- מצופה: `too_late` 400; לא מונע קישור-חברות במקביל (ראה ENT-16).

**ENT-13 · Referrer מקודד כפול (`ref%3DABC1234`)**
- מצופה: `readReferrerParam` מפענח שוב ומשייך בהצלחה.

**ENT-14 · שלוש ניסיונות כשל**
- קדם: חסום רשת בשלוש הפעלות ראשונות.
- מצופה: אחרי 3 ניסיונות האפליקציה מפסיקה לנסות (`MAX_ATTEMPTS`), בלי לופ בכל עלייה, בלי שגיאה למשתמש.

**ENT-15 · קוד בפורמט שגוי בקישור**
- מצופה: `/i/AB` (קצר מדי) — לא נשלח לשרת בכלל (regex 4–16).

### 2.3 קישור חבר (`/f/<CODE>`) — "משתמש כחבר"

**ENT-16 [P0] · אפליקציה מותקנת, המשתמש מחובר**
- צעדים: B פותח `/f/<CODE-של-A>` מ-WhatsApp.
- מצופה: הדפדפן מעביר ל-`once://f/<CODE>`; האפליקציה עולה **בלי לנווט/לרענן מסך** (אין רימאונט של בית, צ'אט פתוח נשאר פתוח); הקישור נצרך; השניים הופכים חברים הדדית בלי אישור; A מקבל פוש `friend_link` ("הצטרף אליך כחבר ב-Once").
- אימות: שורה ב-`friend_links` עם `a=LEAST, b=GREATEST`; שני הצדדים רואים זה את זה ברשימת "החברים שלי".

**ENT-17 [P0] · אפליקציה לא מותקנת**
- קדם: **שני הצדדים `is_test=true`** (סמן את המוזמן לפי ENV-10) — אחרת החברות תיווצר אבל שום קרדיט לא ישולם (ENV-12), וזו לא תקלה.
- צעדים: פתח `/f/<CODE>` במכשיר נקי.
- מצופה: אחרי ~1.2 שנ' ללא הענות → `/download?ref=<CODE>&f=1`; אחרי התקנה ופתיחה ראשונה, המשתמש גם משויך לקרדיט **וגם** מקושר כחבר.
- אימות: `friend_links` + `referrals` שניהם קיימים.

**ENT-18 · קישור חבר כשהמשתמש לא מחובר**
- צעדים: פתח `/f/<CODE>` כשהאפליקציה מותקנת אך המשתמש בלוג-אאוט.
- מצופה: הקוד נשמר (`stashInviteUrl`), מסך התחברות עולה, ואחרי התחברות הקישור נצרך אוטומטית (`flushPendingInvite`).

**ENT-19 · קישור חבר לעצמי**
- מצופה: `bad_code`/דחייה שקטה; לא נוצרת "חברות עם עצמי".

**ENT-20 · קישור חבר כשכבר חברים**
- מצופה: אידמפוטנטי — נשאר חבר אחד, בלי פוש כפול, בלי שגיאה.

**ENT-21 · בוסט חברות במיון**
- קדם: A ו-B חברים, יש עוד מועמדים.
- מצופה: B מקבל ×3 ברלוונטיות ולכן נוטה להופיע ראשון.
- אימות: `select relevance from others(...)` — הפקטור מוכפל.

**ENT-22 · קוד לא תקין / קישור חתוך**
- צעדים: `/f/`, `/f/@@`, `/g/12`.
- מצופה: הדפדפן מנתב ל-`/download` (אין דף שגיאה); אם הגיע לאפליקציה — נבלע בלי קריסה.

### 2.4 קישור קבוצה (`/g/<TOKEN>`) — הזמנה מקבוצה

**ENT-23 [P0] · הזמנה לקבוצה, אפליקציה מותקנת, קבוצה בלי אישור**
- צעדים: OWNER משתף קישור קבוצה; B פותח אותו.
- מצופה: האפליקציה עולה בלי ניווט; B מצורף מיד (`join_status=joined`); "הקבוצות שלי" מציג את הקבוצה; אין פוש לבעלים.
- אימות: שורה ב-`user_groups`.

**ENT-24 [P0] · קבוצה עם אישור (`requires_approval=true`)**
- מצופה: `join_status=pending`; הקליינט מציג "ממתין לאישור" ולא "הצטרפת"; OWNER + כל מנהל מקבלים פוש `group_join` עם שם הקבוצה; לחיצה על הפוש פותחת את הקבוצה (payload נושא `group_id`).

**ENT-25 [P0] · הזמנה לקבוצה, אפליקציה לא מותקנת**
- צעדים: מכשיר נקי פותח `/g/<TOKEN>` → `/download?grp=<TOKEN>` → התקנה → פתיחה ראשונה → התחברות.
- מצופה: ההצטרפות מתבצעת בהפעלה הראשונה בלי שהמשתמש הקליד דבר.
- אימות: `log` מכיל `redeem_invite`; חברות נוצרה.

**ENT-26 · טוקן לא קיים / קבוצה מושבתת**
- ⚠️ **קדם מחייב:** קוד בן 6 ספרות שהומצא עלול להתאים לקבוצה **אמיתית**, וההצטרפות תתבצע בפועל (סעיף 0.1.3, כלל 5). לפני הבדיקה ודא שהקוד לא קיים:
```sql
select id, name, owner_id from groups where invite_code = '<CODE>';  -- חייב להחזיר 0 שורות
```
- מצופה: `invite_invalid`; הודעת "קוד הזמנה לא תקין" בממשק אם הוקלד ידנית; במסלול הדיפ-לינק — כשל שקט וניסיון חוזר עד 3 פעמים.
- אחרי הבדיקה: אמת שלא נוצרה חברות (`select * from user_groups where user_id='<UID>'`).

**ENT-27 · הצטרפות כשכבר חבר בקבוצה**
- מצופה: `join_status=already`; לא נוצרת כפילות; לא נשלח פוש.

**ENT-28 · הקלדת קוד 6 ספרות ידנית**
- קדם: הקוד נלקח מקבוצת `QA-` שנוצרה במחזור הזה. לבדיקת הקוד השגוי — ראה האזהרה ב-ENT-26.
- צעדים: תפריט → קבוצות → שדה קוד → הקלד 6 ספרות → הצטרף.
- מצופה: 5 ספרות = כפתור מושבת; 6 ספרות תקין = הצטרפות/ממתין; קוד שגוי = הודעת שגיאה בשדה.

**ENT-29 · הצטרפות פותחת שער זמינות**
- קדם: משתמש שהיה חסום מצפייה (אם רלוונטי בבילד).
- מצופה: `redeem_invite` **לא** נחסם ע"י `requiresPresence` — משתמש חסום יכול להצטרף ולהשתחרר.

**ENT-30 · בוסט קבוצה משותפת**
- מצופה: שני חברי אותה קבוצה מקבלים ×3; צ'יפ "קבוצה משותפת" מופיע בכרטיס הפרופיל, ולחיצה עליו פותחת פופאפ עם רשימת הקבוצות המשותפות (`app/shared_groups`).

### 2.5 דיפ-לינקים ומצבי מערכת

**ENT-31 [P0] · דיפ-לינק בזמן שהאפליקציה בחזית עם צ'אט פתוח**
- מצופה: הצ'אט לא נסגר, הטיוטה נשמרת, אין רימאונט. (זו המשמעות של `redirectSystemPath` שמחזיר `null`.)

**ENT-32 · דיפ-לינק כשהאפליקציה ברקע**
- מצופה: האפליקציה עולה למצב שהיה, הקוד נצרך ברקע, הודעת/עדכון מתאים.

**ENT-33 · דיפ-לינק בקר-סטארט (Cold start)**
- מצופה: האפליקציה נטענת למסך הפתיחה הרגיל (`/`), ואחרי שיש סשן — הקוד נצרך.

**ENT-34 · שני קישורים ברצף מהיר**
- צעדים: פתח `/g/<T>` ומיד `/f/<C>`.
- מצופה: שניהם נצרכים (לא נדרס אחד ע"י השני), או שהשני נשמר ונצרך אחריו. אין קריסה.

**ENT-35 · לחיצה על פוש `group_join`**
- מצופה: פתיחת האפליקציה במסך הקבוצה הרלוונטית (לא בית סתמי).

**ENT-36 · לחיצה על פוש `chat` בקר-סטארט**
- מצופה: האפליקציה עולה ישר עם שכבת הצ'אט פתוחה (`initialNotif.overlay==='chat'`), בלי הבהוב של הבית.

**ENT-37 · לחיצה על פוש `invite-in`**
- מצופה: כרטיס ההזמנה הנכנסת מוצג מעל הבית עם טיימר חי.

---

## 3. AUTH — הזדהות

**AUTH-01 [P0] · התחברות Google**
- מצופה: זרימת OAuth, חזרה לאפליקציה, ניתוב: חשבון חסר → אונבורדינג; חשבון קיים → בית.

**AUTH-02 · ביטול OAuth באמצע**
- מצופה: חזרה למסך התחברות, כפתורים חוזרים למצב פעיל, בלי ספינר תקוע.

**AUTH-03 · Apple (רק ב-iOS)**
- מצופה: כפתור מוצג רק ב-iOS ונראה זהה לחלוטין לכפתור Google (אותו קומפוננט).

**AUTH-04 [P0] · קישור קסם באימייל**
- צעדים: הקלד אימייל תקין → שלח → פתח את המייל במכשיר → לחץ.
- מצופה: `once://login-callback#access_token=...` נצרך, סשן נוצר, ניתוב אוטומטי; מסך "נשלח קישור אל {email}" מוצג בינתיים.

**AUTH-05 · אימייל לא תקין**
- מצופה: כפתור השליחה מושבת/שגיאה מוצגת; אין קריאה לשרת.

**AUTH-06 · קישור קסם שנפתח בדפדפן אחר / פג תוקף**
- מצופה: הודעת שגיאה ברורה, אפשרות לשלוח מחדש, בלי קריסה.

**AUTH-07 · חשבון בדיקת חנות (review)**
- צעדים: הקלד את כתובת ה-review הקבועה.
- מצופה: נחשף שדה קוד; קוד נכון = התחברות מיידית; קוד שגוי = שגיאה. אימייל רגיל לא חושף את השדה.

**AUTH-08 · שמירת סשן**
- מצופה: סגירה ופתיחה חוזרת נכנסות ישר לבית בלי התחברות מחדש.

**AUTH-09 · טיים-אאוט בעליית סשן**
- קדם: רשת חסומה.
- מצופה: אחרי 5 שניות נופל למסך התחברות (לא נשאר תקוע על מסך ריק).

**AUTH-10 [P0] · יציאה (Sign out)**
- צעדים: תפריט → יציאה → אישור.
- מצופה: חוזר למסך התחברות; טוקן הפוש נמחק; המיקום מתאפס; `page2` מסומן `locked/message=logout`.
- אימות: `select relations->'page2' from users where user_id='<UID>'` → `{"state":"locked","message":"logout"}`.

**AUTH-11 [P0] · חזרה אחרי יציאה**
- מצופה: `app/start` הראשון מחזיר `page2` ל-`{state:'free',profiles:[]}` — המשתמש שוב גלוי.

**AUTH-12 · הסתרה מפורשת נשמרת בין התחברויות**
- קדם: המשתמש הסתיר את עצמו בהגדרות (`hide_me`, בלי `message`).
- מצופה: אחרי יציאה+חזרה הוא **נשאר** מוסתר.

**AUTH-13 · יציאה בזמן שיחה פעילה**
- מצופה: הצד השני מקבל כרטיס `logout` ("כבר לא כאן") ופוש; השיחה נסגרת נקי בשני הצדדים.

**AUTH-14 · יציאה בזמן הזמנה יוצאת ממתינה**
- מצופה: ההזמנה מתפרקת; הנמען מקבל התראה מתאימה; קרדיט מוחזק מטופל (לא נעלם בלי סיבה).

**AUTH-15 [P0] · מחיקת חשבון**
- צעדים: תפריט → מחיקת חשבון → אישור.
- מצופה: השורה נמחקת; חוזרים למסך התחברות; מי שהיה מקושר רואה כרטיס `delete` ("כבר לא כאן").
- אימות: `select count(*) from users where user_id='<UID>'` = 0; `log` מכיל `delete`.

**AUTH-16 · מחיקת חשבון והתחברות חדשה עם אותו אימייל**
- מצופה: נוצר חשבון חדש לגמרי (אונבורדינג מהתחלה), בלי שאריות של הקודם (אין הזמנות/חברים/קבוצות).
- ⚠️ **החשבון החדש נולד `is_test = false` גם אם הקודם היה מסומן** — הדגל נמחק יחד עם השורה. סמן אותו מיד עם יצירתו (סעיף 0.1.2) לפני שממשיכים.

**AUTH-17 · הרשאה שנשללה בצד Supabase (טוקן פג)**
- מצופה: 401 `unauthenticated` מהשרת → הקליינט מנתב להתחברות בלי לופ.

**AUTH-18 · מסך התחברות בזמן העברה (handoff)**
- מצופה: אין הבהוב/ריצוד בין התחברות לבית — שכבת בז' רצופה עד שהיעד מצויר.

---

## 4. ONB — אונבורדינג

**ONB-01 [P0] · חמישה שלבים בסדר**
- מצופה: מין → שם → תאריך לידה → תמונות → ביו. מעבר רק כשהשלב תקין.

**ONB-02 · בחירת מין**
- מצופה: לא ניתן להתקדם בלי בחירה; הבחירה נשמרת בחזרה אחורה.

**ONB-03 · שם**
- מצופה: מקסימום 30 תווים; שם ריק/רווחים = כפתור מושבת; עברית ואנגלית נשמרות נכון.

**ONB-04 [P0] · תאריך לידה וגיל מינימלי**
- מצופה: גיל < 18 → הודעת גיל מינימלי והמשך חסום; 18–120 תקין; תאריך לא חוקי (31/02) נדחה.

**ONB-05 [P0] · יצירת חשבון בשלב 3 → סימון מיידי כמשתמש בדיקות**
- מצופה: `app/account` נשלח עם `name`+`birth_date`+`is_male`; מכאן המשתמש "קיים" גם אם ינטוש.
- **צעד מחייב מיד אחרי:** עוצרים את הבדיקה ומסמנים את החשבון `is_test = true` (סעיף 0.1.2), לפני העלאת תמונות ולפני כל פעולה אחרת.
- אימות: `select user_id, name, is_test from users where user_id='<UID>'` → `is_test = t`, והחשבון עדיין בלי תמונות (כלומר עוד לא היה גלוי לאף אחד).

**ONB-06 [P0] · תמונות — מינימום 2**
- מצופה: כפתור המשך מושבת עד 2 תמונות; אפשר עד 6; מעבר ל-7 חסום.

**ONB-07 · העלאת תמונה נכשלת**
- מצופה: המשתמש מוחזר לשלב התמונות עם הסבר (`failToPhotoStep`), לא ממשיך עם רשימה ריקה.

**ONB-08 · תמונה כבדה / פורמט חריג (HEIC, פנורמה)**
- מצופה: נדחסת ונשמרת, או הודעת שגיאה מנומסת; בלי קריסה.

**ONB-09 · סירוב הרשאת גלריה/מצלמה**
- מצופה: הסבר + כפתור למסך ההגדרות של המערכת; אין קריסה.

**ONB-10 [P0] · ביו 20–150**
- מצופה: פחות מ-20 = חסום; 150 = תקרה קשה; שורות ריקות נמחקות בשמירה (`normalizeBio`).

**ONB-11 · שמירת הביו מסיימת אונבורדינג**
- מצופה: מעבר לבית; הפרופיל נחשב "בנוי" (תמונה ≥1 + ביו לא ריק) ולכן המשתמש גלוי.

**ONB-12 · ניווט אחורה במחווה**
- מצופה: גרירה למטה/כפתור חזור עובדים בשלבים שמותר בהם; שלב 1 לא חוזר לשום מקום.

**ONB-13 · נטישה באמצע וחזרה**
- מצופה: חזרה לאפליקציה מחזירה לשלב שנשמר (למשל 4 אם יש חשבון בלי תמונות).

**ONB-14 [P0] · משתמש browse-only (חשבון בלי פרופיל)**
- מצופה: מגיע לבית; רואה מועמדים; **לא** יכול לשלוח הזמנה — ה-CTA מוביל לפופאפ "בנה פרופיל"; לא מוקצה לו צופה; לא מופיע אצל אחרים.
- אימות שרת: קריאה ישירה ל-`app/invite` מחזירה `profile_incomplete` 403 ונרשמת בלוג.

**ONB-15 · פתיחת אונבורדינג מרצון מהתפריט**
- מצופה: כפתור "בנה פרופיל" (כתום) פותח את שלב התמונות; יציאה/סיום מחזירה לבית; אין ניתוב אוטומטי שמעיף מהזרימה.

**ONB-16 · מקלדת מכסה שדה**
- מצופה: השדה הפעיל תמיד מעל המקלדת בכל שלב (נדחף ע"י `useKeyboardHeight`), בלי "קפיצה כפולה".

**ONB-17 · סיבוב מסך / גופן ענק**
- מצופה: הפריסה לא נשברת; טקסטים לא נחתכים.

**ONB-18 · אין רשת בשלב 3 (יצירת חשבון)**
- מצופה: הודעת שגיאה + אפשרות לנסות שוב; המשתמש לא נתקע במצב חצי-חשבון בלי משוב.

---

## 5. GATE — הרשאות ושער זמינות

**GATE-01 [P0] · הרשאת התראות במצב undetermined**
- מצופה: מסך הבית מציג הודעת "צריך התראות" עם פעמון; לחיצה על המרכז פותחת בקשת הרשאה של המערכת.

**GATE-02 [P0] · דחיית התראות → שער נוכחות**
- צעדים: דחה הרשאה; המשך.
- מצופה: המשתמש לא מוצג לאף אחד; לא יכול `find`/`invite`/`approve`; הצ'אט וההזמנות בלתי-נגישים; **התפריט כן נגיש**.
- אימות: `relations.availability = {state:'unavailable',reason:'push'}`; קריאה ל-`app/find` = `unavailable` 403 בלוג.

**GATE-03 · אישור התראות מאוחר משחרר את השער**
- צעדים: אשר מהגדרות המערכת → חזור לאפליקציה.
- מצופה: `app/notif` נשלח מיד, `availability` הופך `available`, מסך הבית חוזר לנורמלי בלי הפעלה מחדש.

**GATE-04 · טוקן פוש מת (הוסרה האפליקציה/נשלל)**
- קדם: הזרק `relations.push.dead=true`.
- מצופה: אותה חסימה כמו דחיית הרשאה, עם אותה כותרת פעמון; שליחת טוקן חדש מנקה את הסימון.

**GATE-05 [P0] · הרשאת מיקום — undetermined/denied**
- מצופה: הודעת "צריך מיקום" עם סמן מפה; לחיצה פותחת בקשה/מסך הגדרות; אין מועמדים עד שיש מיקום.

**GATE-06 · שירותי מיקום כבויים במכשיר**
- מצופה: כותרת "מיקום לא זמין" נפרדת + פתיחת הגדרות המערכת (Intent).

**GATE-07 · כשל בקבלת קואורדינטות למרות הרשאה**
- מצופה: מצב `locFailed` עם כפתור נסה שוב; לא לופ אינסופי.

**GATE-08 · אין אינטרנט**
- מצופה: כותרת "אין חיבור" + אייקון Wi-Fi כבוי + כפתור ניסיון חוזר; חזרה של הרשת משחזרת אוטומטית.

**GATE-09 · עדיפות המצבים**
- מצופה: הרשאות (התראות → מיקום → רשת) קודמות לשער השרת; רק אחת מוצגת בכל רגע.

**GATE-10 [P0] · התפריט אינו חסום אף פעם**
- מצופה: בכל אחד ממצבי GATE אפשר לפתוח את תפריט ההמבורגר ולשנות מיקום/הגדרות.

**GATE-11 · צ'אט פתוח כשהשער נדלק**
- מצופה: הצ'אט נסגר בכפייה; התפריט/תצוגת הפרופיל נשארים.

**GATE-12 [P0] · חלון נוכחות 24 שעות**
- קדם: `last_seen` של B לפני יומיים.
- מצופה: B לא מופיע כמועמד לאף אחד; פתיחת האפליקציה ע"י B מחזירה אותו לפול מיד.

**GATE-13 · משתמש בלי `location`**
- מצופה: לא מוצג לאחרים (`only_available` דורש location), אבל כן יכול לצפות.

**GATE-14 · חסימת "לא שילם" (`unpaid_at`)**
- קדם: B לחץ אישור בארנק ריק.
- מצופה: כל עוד `unpaid_at` מסומן והארנק ריק, אף אחד לא יכול להזמין את B; קרדיט חדש (מכסה יומית/החזר) מנקה את הסימון אוטומטית.

**GATE-15 · תזמון עתידי (`not_yet`) — legacy**
- מצופה: אם השרת יחזיר `not_yet`, מוצגת הודעת תאריך; לחיצה על האווטר פותחת תצוגת פרופיל. (לא נגיש בבילד הנוכחי — בדיקת רגרסיה בלבד.)

---

## 6. HOME — מסך הבית ושכבות (Shell/Overlays)

**HOME-01 [P0] · מסך אחד בלבד**
- מצופה: אין טאבים, אין פייג'ר, אין ניווט אופקי. הכול עולה כשכבה מעל הבית ונסגר בגרירה למטה.

**HOME-02 · המבורגר צף למעלה-התחלה**
- מצופה: לא זז כשכרטיס נגרר; תמיד לחיץ.

**HOME-03 [P0] · גרירה בכל מקום בשל פותחת תפריט**
- מצופה: גרירה אופקית מכל נקודה בבית פותחת את המגירה בעקבות האצבע; גרירה אנכית לא פותחת תפריט אלא מפעילה skip.

**HOME-04 · המגירה עוקבת אצבע בשני הכיוונים**
- מצופה: פתיחה וסגירה זהות בתחושה; אין "קפיצה"; המגירה לא נטענת מחדש בכל פתיחה.

**HOME-05 · סדר צביעה**
- מצופה: בית < הזמנה < צ'אט < תפריט < תצוגת פרופיל. התפריט תמיד מעל.

**HOME-06 [P0] · כפתור חזור של אנדרואיד**
- מצופה: דיאלוג פתוח נסגר ראשון → שכבה עליונה נסגרת → הזמנה נכנסת (ממתינה = פותחת אישור דחייה; מתה = נבלעת) → יציאה מהאפליקציה.

**HOME-07 · גרירה לסגירת שכבה**
- מצופה: כל שכבה נסגרת בגרירה למטה; הצ'אט נסגר מהכותרת הצפה תמיד, ומהגוף כשאין לאן לגלול/כשהגענו לקצה הכי ותיק.

**HOME-08 · שכבות מוערמות**
- מצופה: תצוגת פרופיל נערמת מעל התפריט; גרירה סוגרת את העליונה בלבד; ה-pan של האב מנוטרל.

**HOME-09 · אין כרום מיותר בבית**
- מצופה: אין מונה קרדיטים, אין מונה צופים, אין בקרת נראות ואין ברודקאסט במסך הבית (הכול בהגדרות).

**HOME-10 · צ'יפ שם/גיל קבוע בפינה למעלה-סוף**
- מצופה: לא נגלל עם הפרופיל; טקסט מודגש; לא מתנגש עם ההמבורגר.

**HOME-11 · כפתור דיווח בתחתית התמונה האחרונה**
- מצופה: עיגול קטן, רקע בז' כהה, סמל סגול עמום; מופיע על התמונה האחרונה בכל כרטיס התאמה; **לא** מופיע בתצוגת הפרופיל של עצמי.

**HOME-12 · לב/צ'אט צף קבוע בתחתית-סוף**
- מצופה: לא נגלל; טקסט על התמונה לא נכנס מתחתיו.

**HOME-13 · צ'יפים על התמונה**
- מצופה: כל צ'יפ הוא אריח בז' עם אותה הצללה כמו הכפתורים העגולים; צ'יפ `solid` (סיום צ'אט) סגול מלא עם טקסט לבן.

**HOME-14 · ביו כאריח בז' בתחתית התמונה השנייה**
- מצופה: לא חוסם את הלב; מתרומם מעל כפתור הדיווח; בתצוגת הפרופיל של עצמי — אותו אריח בדיוק, עם עריכה בתוכו.

**HOME-15 · פרופיל עם תמונה אחת**
- מצופה: הביו מוצג בבועה הלבנה החלופית; הדיווח יושב על התמונה היחידה.

**HOME-16 · גלילת "ריל" בין תמונות**
- מצופה: הצמדה לגבולות תמונה; מושבתת רק כשעורכים ביו במיקוד.

**HOME-17 · מרכז המסך במצבים שונים**
- מצופה: play כשאין מועמד · pause בזמן דילוג · אווטר/סמל במצבי שער · ספינר בזמן טעינה.

**HOME-18 · לחיצת play ראשונה בזמן עלייה**
- מצופה: הלחיצה לא נבלעת — נכנסת לתור ומורצת ברגע שהאתחול הסתיים (ספינר בינתיים).

**HOME-19 · כותרות מתחלפות**
- מצופה: "סורק..." / "טוען פרופיל" / "אין אף אחד בסביבה" / משפט מוכנות רנדומלי, במקום אחד ובאותו עיצוב.

**HOME-20 · חזרה מרקע**
- מצופה: `app/focus` נשלח; הפרופיל נטען מחדש; מגש ההתראות מתנקה; אין הבהוב כרטיס.

---

## 7. FIND — מציאת מועמד, דילוג, מיון

**FIND-01 [P0] · play מביא מועמד**
- מצופה: `app/find` נשלח; כרטיס מועמד עולה עם תמונות, שם/גיל, זמן/מרחק, ילדים/משפחה, קבוצה משותפת (אם יש), ביו.

**FIND-02 · אין מועמדים**
- מצופה: מסך "אין אף אחד בסביבה" + קישור להעדפות חיפוש (טווח/גיל/מין), בלי כרטיס ריק.

**FIND-03 [P0] · חיפוש אוטומטי בעלייה/פוקוס/מיקום**
- מצופה: כשמצב `page1='free'` ואין פרופיל — השרת מוצא מועמד לבד ב-`start`/`focus`/`location`.

**FIND-04 · חיפוש אוטומטי אחרי שינוי העדפות**
- מצופה: שינוי גיל/טווח/מין מועדף/פרופיל (משפחה) מפעיל חיפוש מחדש כשהמשתמש בטל.

**FIND-05 · אין חיפוש אוטומטי כשיש מצב פעיל**
- מצופה: ב-`watching`/`waiting`/`chat`/`locked` השרת **לא** דורס את המצב.

**FIND-06 [P0] · pull-to-skip**
- צעדים: גרור את הכרטיס למטה עד המפתן.
- מצופה: הכרטיס מחליק מהמסך, `app/ignore` נשלח, מיד מתחיל סריקה למועמד הבא; נוצרת הגבלה של יום.

**FIND-07 [P0] · pause באמצע דילוג**
- מצופה: כפתור המרכז עוצר את החיפוש; גם אם השרת כבר מצא מועמד — הוא לא יוצג; `app/pause` נשלח **אחרי** ה-find/ignore שבאוויר, ו-`page1` נשאר `locked`.

**FIND-08 · לחיצת play אחרי pause**
- מצופה: מתחיל חיפוש חדש; ה"נעילה" של הדילוג הקודם משוחררת.

**FIND-09 · ניתוק רשת בזמן find**
- מצופה: שגיאה מטופלת בשקט; הפרופיל נטען מחדש מהשרת כדי לא להשאיר UI לא-מסונכרן.

**FIND-10 [P0] · התאמת מין דו-כיוונית**
- מצופה: מוצגים רק מי שמחפש אותי ואני אותו.

**FIND-11 [P0] · טווח דו-כיווני**
- מצופה: הטווח האפקטיבי הוא ה-`LEAST` של שני הצדדים; מי שמחוץ לטווח של אחד הצדדים לא מוצג.

**FIND-12 · טווח "ללא הגבלה"**
- מצופה: `range=null` → אין סינון מרחק מהצד הזה; המרחק מוצג בצ'יפ בכל מקרה.

**FIND-13 · יחידות מרחק לפי מכשיר**
- מצופה: אזור ארה"ב = מיילים, אחרת ק"מ/מטרים; אין הגדרת יחידות באפליקציה.

**FIND-14 · מיון לפי רלוונטיות**
- מצופה: מועמד עם קבוצה משותפת/חברות/ברודקאסט טרי עולה לפני מועמד ניטרלי דומה.
- אימות: השוואת `relevance` בין השורות ב-`others()`.

**FIND-15 · מועמד ללא תמונות לא מוצג**
- מצופה: `only_available` דורש `images >= 1`.

**FIND-16 · מועמד בצ'אט לא מוצג**
- מצופה: `page1.state='chat'` מוציא מהפול; כך גם `page2` נעול בלי `message`.

**FIND-17 [P0] · חלונות הגבלה**
| פעולה | חלון | בדיקה |
|---|---|---|
| דילוג (`ignore`) | 1 יום | מיד אחרי דילוג הוא לא חוזר; אחרי 24 ש' כן |
| ביטול הזמנה (`cancel`) | 1 יום | |
| הסרת צופה (`remove`) | 1 יום | |
| דחיית הזמנה (`decline`) | 7 ימים | |
| סיום שיחה (`leave`) | 14 יום | |
| חסימה (`block`) | לנצח | גם אחרי חודש לא חוזר |
- מצופה: החסימה דו-כיוונית (גם הצד השני לא רואה אותי).
- אימות: `select key, created_at from restrictions where ...`.

**FIND-18 [P0] · הפרדת משתמשי בדיקה נשמרת בכל מסלול**
- מצופה: `is_test=true` לא רואה `is_test=false` ולהפך — לא ב-`find`, לא בחיפוש אוטומטי, לא בזריעת צופה (`app_seed_viewer`), לא בבוסט קבוצה/חברות, ולא בברודקאסט.
- צעדים: אחרי כל אחד מהמסלולים לעיל, הרץ את שאילתת ENV-09 ואת רשימת הצופים:
```sql
select jsonb_array_elements(relations->'page2'->'profiles')->>'user_id' from users where user_id='<A>';
```
ובדוק שכל מזהה שחוזר שייך לחשבון `is_test=true`.
- **אם נכשל:** תקלה חוסמת (P0) — עוצרים את המחזור, כמו ב-ENV-09.

**FIND-19 · זריעת צופה ראשון**
- קדם: משתמש חדש עם פרופיל בנוי ובלי צופים.
- מצופה: השרת מקצה לו צופה אחד (`app_seed_viewer`) והצופה מקבל פוש `candidate`.

**FIND-20 · זריעה חוזרת אחרי דילוג**
- מצופה: כשהדילוג רוקן את רשימת הצופים של מישהו, הוא מקבל צופה חדש; הצופה החדש לעולם לא הדולג עצמו (בגלל ההגבלה).

**FIND-21 · דילוג רצוף מהיר (5 פעמים)**
- מצופה: כל דילוג נרשם, אין כרטיסים כפולים, אין הבהוב "פרופיל → ריק → פרופיל", אין קריסה.

**FIND-22 · טעינת תמונות איטית**
- מצופה: הכותרת "טוען פרופיל" מוצגת; הכרטיס עולה רק כשהתמונות מוכנות (preloader).

**FIND-23 · תפוגת צפייה (`watching`)**
- מצופה: אחרי חלון התפוגה, הסוויפ השעתי משחרר את הצפייה; המשתמש חוזר למצב חיפוש בלי כרטיס תקוע.

**FIND-24 · שינוי מיקום ידני (בית/עבודה)**
- מצופה: מועמדים מתעדכנים לפי המיקום שנבחר, לא לפי ה-GPS.

---

## 8. INV — הזמנה לצ'אט (page1 → page2)

### 8.1 שליחה

**INV-01 [P0] · פתיחת פופאפ הזמנה**
- צעדים: כרטיס מועמד → לחיצה על הלב.
- מצופה: נפתח פופאפ תחתון עם כותרת מודגשת + הסבר ("שניכם תנעלו... 10 דקות") + עלות 1 קרדיט + כפתור שליחה.

**INV-02 [P0] · שליחת הזמנה**
- מצופה: `app/invite` נשלח; מצב הופך `waiting`; קרדיט אחד יורד ומוחזק (`held`); הכרטיס מציג טיימר 10 דקות + כפתור ביטול; הנמען מקבל פוש `invite-in`.
- אימות: `relations.credits.held=1`, `relations.page1.state='waiting'`, `expires_at ≈ now+10m`.

**INV-03 [P0] · הזמנה בלי קרדיטים**
- קדם: ארנק 0.
- מצופה: לחיצה על שליחה פותחת פופאפ קרדיטים (לא כפתור מעומעם ולא שגיאה); השרת מחזיר `no_credits` אם נשלח בכל זאת.

**INV-04 · פופאפ קרדיטים — כל האפשרויות מושבתות**
- מצופה: 3/10/50 מוצגות עם תג "בקרוב"; לחיצה לא מבצעת רכישה; שורת הזמנת חבר מוצגת כדרך היחידה להשיג קרדיט.

**INV-05 · שליחה ממשתמש בלי פרופיל בנוי**
- מצופה: פופאפ "בנה פרופיל" (לא שליחה); בשרת `profile_incomplete` 403.

**INV-06 [P0] · מיגזע: מישהו הקדים אותי**
- צעדים: A ו-A2 שולחים הזמנה ל-B באותו רגע.
- מצופה: אחד מצליח; השני מקבל `page1` נעול עם `invite-fail`, כרטיס "מישהו הקדים אותך", וקרדיט **לא** יורד לו (או מוחזר).
- אימות: רק הזמנה אחת ב-`page2` של B.

**INV-07 · הזמנה למי שיצא מהפול בין הצפייה לשליחה**
- מצופה: כשל מסודר (`no_target`/`not_watching`) + כרטיס הסבר; אין הזמנה תקועה.

**INV-08 [P0] · ביטול הזמנה יוצאת**
- צעדים: כרטיס `waiting` → ביטול → אישור.
- מצופה: הכרטיס מחליק החוצה מיד (אופטימיסטי) עם הטיימר; `app/cancel` נשלח; **הקרדיט המוחזק מתאבד** (לא מוחזר); הנמען מקבל פוש `cancelled-in` וכרטיס "ההזמנה בוטלה"; נוצרת הגבלת יום.

**INV-09 · ביטול בזמן שהנמען מאשר (מיגזע)**
- מצופה: מנצח אחד בלבד. אם האישור הקדים — נכנסים לצ'אט ולא נשאר "ביטול חצי"; אם הביטול הקדים — הנמען מקבל `page2` נעול `approve`/`cancel` ולא צ'אט ריק.

**INV-10 · דיווח מתוך כרטיס ההזמנה היוצאת**
- 🛑 היעד הוא בהכרח חשבון בדיקה (הכרטיס נוצר דרך המשחק, שמופרד), אך אמת זאת לפני השליחה — הדיווח בלתי הפיך (END-04).
- מצופה: כפתור הדגל פותח אישור עם שדה הערה חופשית; שליחה חוסמת לצמיתות ומפרקת את הקישור.

**INV-11 · טיימר יוצא**
- מצופה: יורד שנייה-שנייה; רק הכרטיס נצבע מחדש (לא כל הבית); ב-00:00 קופא ומסמן "פג תוקף".

**INV-12 · הארכת הזמנה (אם חשוף בממשק)**
- מצופה: `app/extend` מוסיף דקות; הכותרת הופכת "הוארך התוקף"; הנמען מקבל פוש `extended`.

### 8.2 קבלה (הצד המוזמן)

**INV-13 [P0] · כרטיס הזמנה נכנסת**
- מצופה: עולה כשכבה מעל הבית עם שם/תמונות המזמין, טיימר חי, כפתור אישור (עלות 1) וכפתור דחייה. הכרטיס **נגזר מהשרת** — לא מוערם ידנית, ולא ניתן "לסגור אותו סתם".

**INV-14 [P0] · אישור הזמנה**
- מצופה: `app/approve` נשלח; קרדיט אחד יורד; שני הצדדים עוברים למצב `chat`; המזמין מקבל פוש `match`; כרטיס הבית מציג כפתור פתיחת צ'אט.

**INV-15 [P0] · אישור בארנק ריק**
- מצופה: הכפתור פותח פופאפ קרדיטים; אם נשלח בכל זאת — `unpaid_at` מסומן והמשתמש יוצא מהפול עד שיהיה לו קרדיט.

**INV-16 [P0] · דחיית הזמנה בכפתור**
- מצופה: דיאלוג אישור ("בלתי הפיך") → `app/decline`; המזמין מקבל פוש `declined` + כרטיס "בחר/ה לא להמשיך"; **הקרדיט של המזמין מוחזר**; נוצרת הגבלה של 7 ימים.
- אימות: `credits.held=0`, `balance/extra` חזרו.

**INV-17 · דחייה בגרירה למטה**
- מצופה: הגרירה **לא** דוחה ישירות — היא פותחת את אותו דיאלוג אישור והכרטיס קופץ חזרה (commit='snapBack').

**INV-18 · אישור אחרי שהמזמין ביטל**
- מצופה: `approve-fail` — כרטיס "ההזמנה כבר לא זמינה"; המזמין מקבל פוש `approve-fail` אם רלוונטי; אין צ'אט חד-צדדי.

**INV-19 · דיווח מתוך כרטיס ההזמנה הנכנסת**
- מצופה: זמין לפני כל החלטה; חוסם ומפרק.

**INV-20 · הזמנה נכנסת בזמן שער נוכחות**
- מצופה: כשהמשתמש חסום (`overlaysGated`) — כרטיס ההזמנה לא נגיש; אחרי שחרור השער הוא מופיע (אם עוד בתוקף).

**INV-21 · שתי הזמנות נכנסות במקביל**
- מצופה: בלתי אפשרי — נמען נעול להזמנה אחת; השני מקבל `invite-fail`.

**INV-22 · כרטיס הזמנה מתה (`missed`/`fail`)**
- מצופה: מוצג עם הסבר מגונדר לפי מין שני הצדדים, וכפתור יחיד שמנקה (`show_me`/`clear_watch`) ומחזיר את המשתמש לזמינות.

**INV-23 · כפתור חזור על הזמנה מתה**
- מצופה: נבלע (לא סוגר את האפליקציה, לא מוריד את הכרטיס בלי לנקות בשרת).

**INV-24 · טיימר נכנס מגיע ל-00:00 בזמן שהמשתמש מסתכל**
- מצופה: הקליינט קורא מיד ל-`app/focus` → `app_expire_self` → הכרטיס מתחלף למצב "ההזמנה נסגרה" תוך שנייה-שתיים, בלי להמתין לקרון.

**INV-25 · נעילת שני הצדדים**
- מצופה: בזמן הזמנה חיה, שני הצדדים לא מקבלים/שולחים הזמנות אחרות ולא מופיעים בפול.

**INV-26 · אישור/דחייה בלי רשת**
- מצופה: הלחיצה לא "נעלמת" — או שגיאה ברורה או ניסיון חוזר; לא מוצג צ'אט שלא נוצר.

---

## 9. EXP — פג תוקף (Expiry)

**EXP-01 [P0] · פג תוקף אצל השולח (`expired-out`)**
- קדם: הזמנה חיה; זייף `expires_at` לעבר או המתן 10 דקות.
- מצופה: כרטיס "לא הספיק/ה לענות" (מגונדר), טיימר קפוא ב-00:00, **הקרדיט המוחזק חוזר**, פוש `expired-out`.
- אימות: `credits.held=0` ו-`balance/extra` גדלו בהתאם (ללא חריגה מהתקרה).

**EXP-02 [P0] · פג תוקף אצל הנמען (`expired-in`)**
- מצופה: כרטיס "ההזמנה נסגרה" + "אפשר לחזור להיות זמין"; כפתור יחיד מחזיר לזמינות; פוש `expired-in`.

**EXP-03 [P0] · תפוגה כשהאפליקציה סגורה**
- מצופה: הקרון הדקתי (`expire-sweep`) סוגר את ההזמנה; בפתיחה הבאה המשתמש רואה את הכרטיס הסופי, לא הזמנה "חיה" מזויפת.

**EXP-04 · תפוגה בדיוק ברגע האישור**
- מצופה: הנעילה בשרת קובעת — או אישור מוצלח או `approve-fail`; אין מצב "צ'אט בלי צד שני".

**EXP-05 · שעון מכשיר מקדים**
- קדם: הזז שעון המכשיר קדימה 20 דקות.
- מצופה: הקליינט אולי יבקש סגירה, אבל השרת בודק שוב מתחת ללוק ו**לא** סוגר הזמנה חיה.

**EXP-06 · שעון מכשיר מפגר**
- מצופה: הטיימר מציג לא נכון אך הקרון סוגר בזמן; המשתמש לא מצליח לאשר הזמנה שפגה.

**EXP-07 · החזר קרדיט לא חורג מהתקרה**
- קדם: מכסה יומית מלאה (1) + הזמנה שפגה שנשלחה מקרדיט יומי.
- מצופה: ההחזר לא יוצר 2 במכסה היומית; קרדיט שנקנה חוזר ל-`extra`.

**EXP-08 · מחזור hold+refund חוזר**
- מצופה: לא ניתן לצבור קרדיטים ע"י שליחה+תפוגה חוזרת.

**EXP-09 · תפוגה בזמן שהמשתמש בתפריט**
- מצופה: כשחוזרים לבית הכרטיס כבר במצב הסופי; אין שני כרטיסים.

**EXP-10 · שתי תפוגות בו זמנית (שני הצדדים פתוחים)**
- מצופה: כל צד רואה את הכרטיס שלו; אין פוש כפול ואין קריסה.

**EXP-11 · תפוגת צפייה (לא הזמנה)**
- מצופה: הסוויפ השעתי משחרר `watching` ישן; המשתמש רואה מסך חיפוש רגיל.

**EXP-12 · אינווריאנט לוג בתפוגה**
- אימות: לכל `app_expire_self`/סוויפ יש רשומת לוג; אין תפוגה "שקטה" בלי עקבות.

---

## 10. CHAT — שיחת צ'אט (כל השיחה)

### 10.1 פתיחה, שכבה, מחוות

**CHAT-01 [P0] · פתיחת צ'אט מהכרטיס**
- מצופה: כפתור הצ'אט הצף פותח את שכבת הצ'אט; אין כותרת/סרגל עליון, רק X צף; ההודעות מתחילות מקצה המסך העליון וגוללות מתחת לשורת המצב.

**CHAT-02 · סגירה ב-X הצף**
- מצופה: השכבה נסגרת בגרירה/לחיצה; חוזרים לבית עם כרטיס ההתאמה.

**CHAT-03 [P0] · גרירה לסגירה בגוף הרשימה**
- מצופה: כשהרשימה ריקה/קצרה מהמסך — גרירה בגוף סוגרת; כשהגענו לקצה הוותיק ואין עוד היסטוריה — המשך גרירה סוגר; באמצע שיחה — גרירה גוללת ולא סוגרת (סוגרים מהכותרת).

**CHAT-04 · אין הצטלבות עם השכבות האחרות**
- מצופה: פתיחת תפריט מעל צ'אט עובדת; ה-pan של הצ'אט מנוטרל כשהתפריט עליון.

**CHAT-05 · badge "לא נקרא" על כפתור הצ'אט**
- מצופה: מופיע כשיש הודעה חדשה והצ'אט סגור; מתאפס בפתיחה.

### 10.2 שליחת תוכן

**CHAT-06 [P0] · הודעת טקסט**
- מצופה: בועה אופטימיסטית מיד (סטטוס "ממתין") → V אחת כשנשמר → V כפולה כשנקרא; הצד השני רואה תוך פחות משנייה ב-Realtime.
- אימות: `select * from chat order by created_at desc limit 1` — `user_id`, `other_id`, `text`.

**CHAT-07 · טקסט ריק/רווחים**
- מצופה: כפתור השליחה לא שולח; השרת מחזיר `no_content` 400 אם נשלח ידנית.

**CHAT-08 · טקסט ארוך / רב-שורות**
- מצופה: השדה גדל עד 10 שורות ואז גולל; הבועה עוטפת נכון (RTL/LTR); אימוג'י ורצף לינקים לא שוברים פריסה.

**CHAT-09 [P0] · תמונה**
- צעדים: תפריט צירוף → תמונה → בחר.
- מצופה: העלאה ל-`chat-images` תחת `<uid>/...`, בועה עם תצוגה מקדימה, URL חתום ל-24 שעות, שמירה במטמון מקומי.
- אימות: `image_key` מתחיל ב-`<uid>/`; מפתח של מישהו אחר מוחזר 403 `invalid_image_key`.

**CHAT-10 · כשל בהעלאת תמונה + ניסיון חוזר**
- מצופה: הבועה מסומנת כנכשלה עם אפשרות לנסות שוב; ניסיון חוזר לא יוצר כפילות.

**CHAT-11 [P0] · הקלטת אודיו**
- מצופה: הקלטה עם ויזואליזציה חיה; שליחה מייצרת בועה עם גל (60 עמודות) + משך; נגינה/עצירה עובדות; מעבר אוטומטי להודעה הבאה עם צליל מעבר.
- אימות: `audio_key` תחת `<uid>/`, `audio_bars` באורך 60 בטווח 0..1, `audio_duration_ms` חיובי וקטן מ-24 שעות.

**CHAT-12 · ביטול הקלטה**
- מצופה: החלקה/כפתור ביטול מוחקים בלי לשלוח; אין קובץ מיותר בסטורג'.

**CHAT-13 · הקלטה בלי הרשאת מיקרופון**
- מצופה: בקשה/הסבר; אין קריסה.

**CHAT-14 [P0] · שליחת מיקום**
- מצופה: תפריט צירוף → מיקום → דיאלוג אישור → בועה עם מפה/קואורדינטות; לחיצה פותחת אפליקציית מפות.
- אימות: `location = {lat,lng}` מספריים.

**CHAT-15 [P0] · שליחת לוח זמנים (רק להורים)**
- קדם: `data.family.hasKids=true`.
- מצופה: בורר שבועות (1–4) × 7 ימים; חייב לפחות תא אחד מסומן; נשלח כתצלום קפוא.
- אימות: `schedule={anchor:'yyyy-mm-dd',weeks:[[7 בוליאנים]...]}`.

**CHAT-16 [P0] · לוח זמנים ממשתמש בלי ילדים**
- מצופה: האפשרות לא מוצגת; קריאה ישירה מחזירה `schedule_not_allowed` 403.

**CHAT-17 · לוח זמנים לא תקין**
- מצופה: 5 שבועות / שבוע באורך 6 / הכול false → נדחה (לא נשמר).

**CHAT-18 [P0] · תשובה להודעה (reply)**
- מצופה: החלקה/לחיצה ארוכה על בועה → תצוגה מקדימה מעל שדה הקלט → נשלח עם `reply_to`; הבועה מציגה ציטוט; לחיצה על הציטוט גוללת למקור.
- אימות: `reply_to.preview` עד 140 תווים; `kind` מתוך text/image/audio/location/schedule; `user_id` חייב להיות אחד משני המשתתפים (צד שלישי נדחה).

**CHAT-19 · ביטול reply לפני שליחה**
- מצופה: X בתצוגה המקדימה מסיר; ההודעה נשלחת בלי ציטוט.

**CHAT-20 · תוכן משולב**
- מצופה: תמונה+טקסט / אודיו+reply נשמרים ומוצגים נכון.

### 10.3 קבלה, סנכרון, קריאה

**CHAT-21 [P0] · הודעה נכנסת ב-Realtime**
- מצופה: מופיעה תוך פחות משנייה בלי רענון; הרשימה גוללת לתחתית אם המשתמש שם, ולא "קופצת" אם הוא קורא היסטוריה.

**CHAT-22 · מנגנון פולינג גיבוי**
- קדם: חסום Realtime (WS).
- מצופה: הודעות עדיין מגיעות בפולינג; אין כפילויות (דדופ לפי `created_at` שהקליינט שלח).

**CHAT-23 [P0] · אישורי קריאה**
- מצופה: פתיחת הצ'אט מעדכנת `chat_reads.last_read_at`; אצל השולח ה-V הופך כפול; נשמר גם אחרי סגירת אפליקציה.

**CHAT-24 · "מקליד/ה..."**
- מצופה: מופיע בזמן הקלדה של הצד השני; נעלם אחרי הפסקה; לא נשאר תקוע.

**CHAT-25 · אינדיקציית מחובר**
- מצופה: מתעדכן לפי presence; יציאה מהצ'אט מכבה תוך זמן קצר.

**CHAT-26 [P0] · דפדוף היסטוריה**
- מצופה: 100 הודעות ראשונות; גלילה למעלה טוענת עוד; מקום הגלילה נשמר (אין "קפיצה"); כשאין עוד — מפסיק לנסות.

**CHAT-27 · מפרידי תאריך**
- מצופה: "היום"/"אתמול"/תאריך; נכון גם בחצות ובמעבר יום בזמן שהצ'אט פתוח.

**CHAT-28 · שעות הודעות**
- מצופה: פורמט מקומי, 24 שעות בעברית; זהה בשני הצדדים בהתאם לאזור הזמן.

**CHAT-29 · מטמון מקומי**
- מצופה: פתיחה חוזרת מציגה את השיחה מיד מהמטמון, ואז מסתנכרן; אין מסך ריק.

**CHAT-30 · מצב אופליין**
- מצופה: שליחה במצב טיסה מסומנת כממתינה/נכשלת בבירור; חזרת רשת שולחת או מאפשרת ניסיון חוזר; אין הודעות "רפאים" בצד השני.

**CHAT-31 · תמונות/אודיו במטמון בין הפעלות**
- מצופה: לא מורידים מחדש כל פתיחה; קובץ שנמחק נטען שוב בשקט.

**CHAT-32 · מקלדת ושדה קלט**
- מצופה: השדה תמיד מעל המקלדת; שדה עם 10 שורות לא מכסה את ההודעה האחרונה; סגירת מקלדת מחזירה פריסה.

**CHAT-33 · RTL/LTR מעורב**
- מצופה: בועה עברית מיושרת נכון, אנגלית/מספרים/אימוג'י בתוך טקסט עברי לא הופכים סדר, קישור נלחץ נכון.

**CHAT-34 · פוש בזמן שהצ'אט ברקע**
- מצופה: פוש `chat` עם שם השולח בכותרת; לחיצה פותחת ישר את הצ'אט; אין פוש כשהאפליקציה בחזית והצ'אט פתוח; מגש ההתראות מתנקה בחזרה לחזית.

**CHAT-35 · הרבה הודעות מהירות (20 ברצף)**
- מצופה: כולן מגיעות בסדר הנכון; אין כפילויות; אין קריסה; הגלילה שורדת.

**CHAT-36 · השותף מוחק חשבון בזמן שיחה**
- מצופה: הצ'אט נסגר עם כרטיס "כבר לא כאן"; אין שליחה למקום ריק.

---

## 11. END — סיום שיחה, חסימה, דיווח

**END-01 [P0] · סיום שיחה ("End Chat")**
- צעדים: כרטיס הבית → צ'יפ "סיום צ'אט" ליד השם → פופאפ → סיום.
- מצופה: `app/leave`; שני הצדדים יוצאים מהצ'אט; הצד השני מקבל פוש `left` + כרטיס "השיחה הסתיימה"; נוצרת הגבלת 14 יום; החזר קרדיט **לא** מתבצע.

**END-02 · הצד השני סיים**
- מצופה: כרטיס "יצא/ה מהשיחה" עם כפתור המשך (`clear_watch`), ואז חזרה לחיפוש.

**END-03 [P0] · חסימה**
- מצופה: `app/block`; הזוג נחסם לנצח; אין דרך לראות זה את זה שוב; השיחה נסגרת בשני הצדדים.
- אימות: `restrictions` עם `key='block'`.

**END-04 [P0] · דיווח על משתמש**
- 🛑 **רק מול חשבון בדיקה.** `app_report` אינו מסנן `is_test`: דיווח על משתמש אמיתי יוצר חסימה **לצמיתות** ורשומת תלונה שתגיע לאדמין כאילו הייתה אמיתית. אין ל-`block` חלון תפוגה.
- קדם: שני הצדדים `QA-`; ודא לפני השליחה מול מי אתה מדווח (`select name, is_test from users where user_id='<TARGET>'`).
- מצופה: דיאלוג עם סיבה + הערה חופשית; שליחה יוצרת רשומת דיווח, חוסמת לצמיתות, ומפרקת את הקשר בהתאם למצב (צפייה/הזמנה/צ'אט).
- אימות: רשומה בטבלת הדיווחים + `restrictions` block; מופיע באדמין הווב. **המלץ להתחיל את ההערה ב-`QA:`** כדי שהדיווח יהיה מובחן באדמין.

**END-05 · דיווח מכל סוג כרטיס**
- 🛑 אותה מגבלה כמו END-04 — בכל אחד מהמשטחים, היעד חייב להיות חשבון בדיקה.
- מצופה: זמין בצפייה, בהזמנה יוצאת, בהזמנה נכנסת, בצ'אט, ובכרטיס מצב מת. לא זמין בתצוגת הפרופיל של עצמי.

**END-06 · דיווח בלי הערה**
- מצופה: מותר; נשמר בלי `note`.

**END-07 · סיום/חסימה בלי רשת**
- מצופה: שגיאה מטופלת; אין "סיום מדומה" שמשאיר את הצד השני בצ'אט.

**END-08 · סיום ואז חיפוש מיד**
- מצופה: המשתמש חוזר לפול; השותף הקודם לא מוצע לו 14 יום.

**END-09 · הסרת צופה**
- מצופה: `app/remove` מוציא צופה מהרשימה; הצופה מקבל כרטיס "כבר לא זמין/ה לך" + פוש `removed`; הגבלת יום.

**END-10 · הסתרה מוציאה את כל הצופים**
- מצופה: `hide_me` שולח `remove` לכל הצופים; מונה הצופים בהגדרות מתאפס.

**END-11 · חזרה לזמינות**
- מצופה: `show_me` מחזיר לפול; אם הארנק ריק — השורה מפנה לפופאפ קרדיטים במקום להיכשל בשקט.

---

## 12. COMM — קבוצות וחברים

> ℹ️ **הסקציה שהייתה המסוכנת ביותר, ונחסמה בשרת ב-2026-07-27** (סעיף 0.1.3): חיפוש
> אנשים וחיפוש קבוצות מסוננים לפי מחיצה, ובקשת חברות / קישור חבר / הצטרפות לקבוצה
> דוחים פעולה חוצת-מחיצות לפני שנכתב משהו ולפני שנשלח פוש.
>
> **הכללים נשארים בתוקף כהגנה בשכבה שנייה:**
> 1. **מחפשים רק `QA-`** — אנשים וקבוצות. תוצאה זרה לא נלחצת (וממילא לא תחזור).
> 2. **הקבוצות שנבדקות הן רק קבוצות שנוצרו במחזור הזה ע"י `QA-OWNER`.**
> 3. **בסוף הסקציה מריצים את ENV-13** (גלאי דליפה) ומאמתים 0 שורות.
> 4. תוצאה ריקה בחיפוש אחרי שם/קבוצה אמיתיים היא **התנהגות נכונה**, לא באג.

### 12.1 חברים

**COMM-01 [P0] · רשימת "החברים שלי"**
- מצופה: תפריט → קהילות → החברים שלי; מציג חברים + בקשות ממתינות; מונה מדויק.

**COMM-02 [P0] · חיפוש אנשים**
- 🛑 **חפש רק את התחילית `QA-`.** `app_search_people` מסנן לפי שם בלבד ובלי `is_test` — חיפוש שם גנרי יחזיר משתמשים אמיתיים.
- מצופה: מחרוזת בת תו אחד לא שולחת בקשה (מינימום 2 תווים); `QA-` מחזיר רק חשבונות בדיקה; "אין תוצאות" מוצג נכון; חשבון שכבר חבר או שנשלחה אליו בקשה מסומן כך בתוצאה.
- אימות: הצלב כל `user_id` שחזר מול `select is_test from users where user_id='...'` → `t`.

**COMM-03 [P0] · בקשת חברות**
- 🛑 **רק מול חשבון `QA-`.** בקשה למשתמש אמיתי שולחת לו פוש ומשאירה בקשה ממתינה בממשק שלו. הפוש אינו הפיך.
- מצופה: `friend_request` → היעד מקבל פוש `friend_request`; אצל השולח מוצג "ממתין".

**COMM-04 [P0] · אישור/דחיית בקשת חברות**
- מצופה: אישור → שני הצדדים חברים + פוש `friend_accept` לשולח; דחייה → הבקשה נעלמת בשקט.

**COMM-05 · בקשה כפולה / בקשה למי שכבר חבר**
- מצופה: `bad_target`/אידמפוטנטי, בלי כפילות ובלי פוש נוסף.

**COMM-06 · הסרת חבר**
- מצופה: `unfriend` דו-כיווני; הבוסט ×3 נעלם מהמיון.

**COMM-07 · שיתוף קישור חבר מהתפריט**
- מצופה: תיבת שיתוף עם `/f/<CODE>` + טקסט; מוצג גם התגמול.

**COMM-08 · חברות דרך קישור מול חברות דרך בקשה**
- מצופה: קישור = מיידי בלי אישור; חיפוש = דורש אישור. שתי הדרכים מגיעות לאותה רשימה.

### 12.2 קבוצות — חבר

**COMM-09 [P0] · הקבוצות שלי**
- מצופה: רשימה עם שם קבוצה, מספר חברים, מנהל; מצב "ממתין לאישור" מסומן בתג.

**COMM-10 [P0] · חיפוש קבוצות ציבוריות**
- 🛑 **חפש רק `QA-`.** `app_search_groups` מחזיר **כל** קבוצה ציבורית במערכת, כולל קבוצות של משתמשים אמיתיים. אין להצטרף לתוצאה שלא נוצרה במחזור הזה.
- קדם: `QA-OWNER` יצר לפחות שתי קבוצות ציבוריות `QA-` בגדלים שונים.
- מצופה: מיון לפי מספר חברים (הגדולה ראשונה), תמונת בעלים ותיאור מוצגים, קבוצה פרטית לא מופיעה, מינימום 2 תווים לחיפוש.

**COMM-11 · הצטרפות לקבוצה ציבורית בלי אישור**
- 🛑 רק לקבוצת `QA-` מהמחזור הזה.
- מצופה: הצטרפות מיידית; הרשימה מתעדכנת בלי רענון.

**COMM-12 [P0] · הצטרפות לקבוצה עם אישור**
- 🛑 רק לקבוצת `QA-` מהמחזור הזה. הצטרפות לקבוצה אמיתית שולחת פוש `group_join` לבעלים ולכל המנהלים שלה.
- מצופה: מצב "ממתין"; אפשרות לבטל בקשה (`cancel_join`) עם דיאלוג; ביטול חוזר = no-op מוצלח.

**COMM-13 [P0] · עזיבת קבוצה**
- מצופה: דיאלוג אישור → `leave_group`; אידמפוטנטי; הרשאות ניהול שהיו למשתמש בקבוצה מתבטלות אוטומטית.

**COMM-14 · צ'יפ קבוצה משותפת בכרטיס פרופיל**
- מצופה: מוצג רק כשיש קבוצה משותפת; לחיצה פותחת פופאפ עם כל הקבוצות המשותפות, בעלים ומספר חברים; נטען במקביל לאנימציית הפתיחה.

**COMM-15 · השפעת קבוצה על זמינות**
- מצופה: היום אין חסימה על "בלי קבוצות" — משתמש בלי קבוצה עדיין זמין (רגרסיה של הכלל הישן).

### 12.3 קבוצות — בעלים/מנהל

**COMM-16 [P0] · יצירת קבוצה**
- 🛑 **שם הקבוצה חייב להתחיל ב-`QA-`** (סעיף 0.1.3, כלל 3) — כך היא מובחנת בחיפוש ולא נגררת לתוצאות של משתמשים אמיתיים, ובעיקר: כך אנחנו יודעים שמותר להצטרף אליה.
- מצופה: שם (חובה, ולידציה `bad_name`), תיאור (אופציונלי, `bad_description` לחריגה), פרטית/ציבורית, דורשת אישור או לא; היוצר הוא בעלים.

**COMM-17 · עריכת שם/תיאור**
- מצופה: `update_group` שומר; שדה ריק בתיאור מנקה אותו; לא-מנהל מקבל `not_manager`.

**COMM-18 · החלפת פרטית/ציבורית**
- מצופה: ציבורית מופיעה בחיפוש, פרטית לא; ההערה בממשק מסבירה את המשמעות.

**COMM-19 · הפעלת/כיבוי "דורשת אישור"**
- מצופה: כיבוי → הצטרפות מיידית; הפעלה → בקשות מצטברות; בקשות שהיו ממתינות לא נעלמות.

**COMM-20 [P0] · רשימת חברים וניהול**
- מצופה: `group_members` רק למנהל/בעלים (אחרת `not_manager`); הסרת חבר עובדת; לא ניתן להסיר בעלים (`cant_remove_owner`).

**COMM-21 [P0] · מינוי/ביטול מנהל**
- מצופה: רק בעלים ממנה (`owner_only`); לא ניתן למנות את הבעלים (`is_owner`); לא-חבר נדחה (`not_member`); מנהל חדש רואה מיד את הבקשות.

**COMM-22 [P0] · בקשות הצטרפות**
- מצופה: `group_requests` מציג ממתינים; אישור → הצטרפות + פוש `group_approved` למבקש; דחייה → שקטה; בקשה שנעלמה = `no_request`.

**COMM-23 · פוש לכמה מנהלים**
- מצופה: כל המנהלים + הבעלים מקבלים `group_join`, עם שם הקבוצה מוזרק לטקסט.

**COMM-24 [P0] · מחיקת קבוצה**
- מצופה: רק בעלים (`not_owner`); דיאלוג אישור; החברות של כולם נעלמת; הקישור/הקוד מפסיק לעבוד (`invite_invalid`).

**COMM-25 · שיתוף/רענון קוד הזמנה**
- מצופה: שיתוף מייצר `/g/<TOKEN>`; רענון קוד מבטל את הישן.

**COMM-26 · סיכום קהילות בתפריט**
- מצופה: שורת הסיכום (חברים/קבוצות/בקשות) מתעדכנת אחרי כל שינוי — כולל אחרי חזרה מרקע (הפרופיל נטען מחדש בכל foreground).

**COMM-27 [P0] · סגירת הסקציה — גלאי דליפה**
- צעדים: בסיום כל סקציית COMM הרץ את שלוש השאילתות של **ENV-13**.
- מצופה: 0 שורות בשלושתן — אף חברות, חברות-בקבוצה או הגבלה לא מקשרת חשבון בדיקה למשתמש אמיתי.
- **אם חוזרות שורות:** עצור, נקה (בטל חברות / צא מהקבוצה / מחק הגבלה), וזהה איזה מקרה יצר את הדליפה.

---

## 13. SET — תפריט, פרופיל, קרדיטים, חשבון

**SET-01 · פתיחת התפריט**
- מצופה: מגירה מקצה ההתחלה, עוקבת אצבע, נגישה תמיד.

**SET-02 [P0] · עריכת תמונות**
- מצופה: הוספה/החלפה/סידור/מחיקה; מינימום 2 נאכף (הודעת "צריך לפחות שתיים"); מקסימום 6; התמונה הראשונה היא האווטר.

**SET-03 · עריכת ביו**
- מצופה: 20–150; שורות ריקות נמחקות; שמירה מתעדכנת בתצוגה המקדימה מיד.

**SET-04 [P0] · תצוגה מקדימה של הפרופיל**
- מצופה: נערמת מעל התפריט; נראית **זהה** לכרטיס התאמה (אותו אריח ביו בז'), בלי כפתור דיווח.

**SET-05 [P0] · משפחה/ילדים**
- מצופה: יש/אין ילדים, מספר ילדים וגילים, "רוצה ילדים משלי", לוח זמנים (עד 4 שבועות); הכול נשמר תחת `data.family` ומשפיע על מיון (`kids_preference_match`, `schedule_overlap`).

**SET-06 · טווח גילים**
- מצופה: `age_from<=age_to`; שינוי מפעיל חיפוש מחדש; נשמר.

**SET-07 · מין מועדף**
- מצופה: גברים/נשים/שניהם; אי אפשר לבטל את שניהם (או שיש טיפול מוגן).

**SET-08 · טווח מרחק**
- מצופה: מ"כאן" עד "ללא הגבלה"; יחידות לפי מכשיר; שינוי מפעיל חיפוש מחדש.

**SET-09 [P0] · מיקום: מכשיר / בית / עבודה**
- מצופה: בחירת מכשיר מבקשת GPS; בית/עבודה נשמרים עם כתובת (השלמה אוטומטית); מצב "מיקום נעול" מוסבר; מיקום ידני משנה מועמדים.

**SET-10 · חיפוש כתובת בלי תוצאות / בלי רשת**
- מצופה: הודעה מתאימה; לא נשמר מיקום שגוי.

**SET-11 [P0] · נראות (גלוי/מוסתר)**
- מצופה: מתג יחיד; הסתרה מבקשת אישור ומעיפה צופים; מונה צופים מוצג בצ'יפ; ארנק ריק מפנה לפופאפ קרדיטים במקום להיכשל.

**SET-12 [P0] · קרדיטים**
- מצופה: מוצגת יתרה יומית + קנויים; מוצג מועד המכסה הבאה (20:00 שעון ישראל) בפורמט מקומי; אפשרויות רכישה מושבתות עם "בקרוב".

**SET-13 · חידוש מכסה יומית**
- קדם: יתרה 0, המתן/זייף `next_grant_at`.
- מצופה: היתרה עולה ל-1 בלבד (תקרה), הסימון `unpaid_at` מתנקה, והמשתמש חוזר לפול.

**SET-14 · מונה חברים שהצטרפו**
- קדם: המזמין והמוזמן **באותה מחיצה** (`is_test=true` לשניהם) — ראה ENV-12.
- מצופה: עולה ב-1 לכל מוזמן שהשלים פרופיל, מתעדכן ב-Realtime. זוג חוצה-מחיצות לא מקדם את המונה (מצופה, לא באג).

**SET-15 · שפה**
- מצופה: החלפה he/en מחליפה את כל הטקסטים והכיווניות; אין מקף ארוך (—) באף מחרוזת; טקסטים מגונדרים נכון לפי מין המשתמש ומין הצד השני.

**SET-16 [P0] · דיווח באג**
- 🛑 **פתח את הטקסט ב-`QA:`** — הדיווח מגיע לאותו מסך אדמין שבו מופיעים דיווחי משתמשים אמיתיים (סעיף 0.1.3, כלל 8).
- מצופה: טקסט חופשי + צירוף תמונה אופציונלי; נשמר (בקט `bug-attachments`); לא נוגע במצב המשחק/קרדיטים; טקסט ריק נדחה (`empty`).

**SET-17 · פרטי חשבון**
- מצופה: אימייל ותאריך לידה מוצגים; מה שלא ניתן לעריכה לא נראה עריך.

**SET-18 · קישורי מסמכים משפטיים**
- מצופה: תקנון/פרטיות/בטיחות ילדים נפתחים בדפדפן עם `?lang=` נכון.

**SET-19 · יציאה ומחיקת חשבון**
- מצופה: שני דיאלוגי אישור נפרדים ומובחנים; מחיקה מדגישה שהמעשה סופי.

**SET-20 · שדה קלט מכוסה במקלדת בתוך שיט**
- מצופה: כל פופאפ עם שדה (הערת דיווח, קוד קבוצה, תיאור קבוצה, ביו) נדחף בדיוק פעם אחת מעל המקלדת — לא כפול ולא חסר.

---

## 14. PUSH — התראות

**PUSH-01 [P0] · רישום טוקן**
- מצופה: אחרי אישור הרשאה, טוקן Expo נשמר ונשלח ב-`app/start`; ערוץ `default` נוצר באנדרואיד.

**PUSH-02 · טוקן מתחלף**
- מצופה: טוקן חדש נשלח לשרת ומנקה סימון `dead`.

**PUSH-03 [P0] · קטלוג התראות**
בדוק לכל קוד: כותרת = **שם השולח**, גוף = טקסט המצב, גינדור נכון (זכר/נקבה):
`invite-in`, `candidate`, `match`, `declined`, `expired-out`, `expired-in`, `cancelled-in`, `removed`, `left`, `extended`, `invite-fail`, `approve-fail`, `chat`, `referral`, `friend_request`, `friend_accept`, `friend_link`, `group_join` (עם שם קבוצה), `group_approved` (עם שם קבוצה).

**PUSH-04 · אין התראה לעצמי**
- מצופה: פעולה של המשתמש לא מייצרת פוש אליו.

**PUSH-05 [P0] · דיכוי בחזית**
- מצופה: כשהאפליקציה בחזית אין באנר/צליל/באדג'; העדכון מגיע ב-Realtime בלבד.

**PUSH-06 [P0] · ניקוי מגש**
- מצופה: פתיחה/חזרה לחזית מנקה את כל ההתראות של האפליקציה.

**PUSH-07 · collapse**
- מצופה: כמה התראות מאותו שולח/קבוצה מתקפלות (`collapseId`).

**PUSH-08 · טוקן מת (`DeviceNotRegistered`)**
- מצופה: השרת מסמן `push.dead` ומחשב זמינות מחדש; המשתמש יוצא מהפול עד שיחזור.

**PUSH-09 · פוש לא מעכב תשובת HTTP**
- אימות: זמן תשובת `app/invite` לא נפגע מכשל פוש (הוא רץ ב-`waitUntil`).

**PUSH-10 · פוש בזמן מסך נעול**
- מצופה: מוצג עם הטקסט הנכון; לחיצה פותחת את היעד הנכון.

**PUSH-11 · שפה בהתראות**
- מצופה: לפי `data.lang` של הנמען, לא של השולח.

---

## 15. RT — Realtime, אופליין וסנכרון מצב

**RT-01 [P0] · עדכון `relations` ב-Realtime**
- מצופה: כל שינוי מצב (הזמנה, אישור, תפוגה, קרדיטים) מגיע בלי רענון.

**RT-02 · סדר אירועים**
- מצופה: אין ריצוד "פרופיל → ריק → פרופיל" בלחיצת play (מנגנון `last_seen` ordering).

**RT-03 · התחברות מחדש אחרי נפילת WS**
- מצופה: מנוי מתחדש; מצב מסונכרן; אין הודעות/מצבים אבודים.

**RT-04 · חזרה מרקע ארוך**
- מצופה: `app/focus` + טעינת פרופיל מלאה; מצבים שהתיישנו (הזמנה שפגה) מוצגים סופיים.

**RT-05 · שני מכשירים לאותו חשבון**
- מצופה: פעולה במכשיר אחד משתקפת בשני; אין מצב סותר.

**RT-06 · מצב טיסה ממושך**
- מצופה: מסך "אין חיבור"; חזרת רשת משחזרת אוטומטית בלי הפעלה מחדש.

**RT-07 · הריגת האפליקציה באמצע פעולה**
- מצופה: השרת אטומי — או שהמצב עבר או שלא; אין "חצי הזמנה".

---

## 16. SRV — שרת, DB, לוגים, תאימות לאחור

**SRV-01 [P0] · אינווריאנט לוג**
- צעדים: הרץ 20 פעולות שונות (כולל כשלים ו-403).
- מצופה: **בדיוק** שורת `log` אחת לכל בקשת HTTP, כולל דחיות (`unavailable`, `profile_incomplete`, `no_credits`).
- אימות: השוואת מספר בקשות למספר שורות.

**SRV-02 · כל טרנזיציה = RPC אחד**
- אימות: לפעולת מצב יש רשומה אחת של `app_*` ולא סדרת `from(...)`.

**SRV-03 · אין דדלוק בפעולות מקבילות**
- צעדים: הרץ אישור וביטול בו-זמנית מול אותו זוג, 20 חזרות.
- מצופה: אין `deadlock detected` בלוגים; תמיד תוצאה עקבית.

**SRV-04 · מסלול קריטי קצר**
- מצופה: פוש/לוגים/סנכרון תמונות רצים ב-`waitUntil`; זמן תשובה לפעולה טיפוסית נשאר קטן (מדוד ורשום מדידה).

**SRV-05 · שער `requiresPresence`**
- מצופה: `find`/`invite`/`add`/`approve` נחסמים לחסום; `clear_watch`/`clear_invite`/`decline`/`cancel`/`leave`/`show_me`/`hide_me`/`pause`/`logout`/`ignore` **לא** נחסמים.

**SRV-06 · שער `requiresProfile`**
- מצופה: `invite`/`add` דורשים תמונה + ביו.

**SRV-07 · ולידציית קלט**
- מצופה: `location` לא מספרי = `invalid_location`; `count` לא מתוך 3/10/50 = `bad_count`; `minutes` לא מספר = `bad_minutes`; חסרי מזהה = `no_group_id`/`no_user_id`/`no_request_id`.

**SRV-08 · הרשאות בין-משתמשים**
- מצופה: מפתח תמונה/אודיו של משתמש אחר = 403; קריאת חברי קבוצה שאינך מנהל בה = `not_manager`; שינוי קבוצה של אחר נדחה.

**SRV-09 · תאימות לאחור — `set_tier`**
- מצופה: `POST /app/set_tier {tier:'pro'}` מחזיר הצלחה ולא משנה ארנק (no-op) — בילדים ותיקים לא מקבלים שגיאה.

**SRV-10 · תאימות לאחור — `units`**
- מצופה: `POST /app/units` מחזיר 200 והשדה נזרק; אין קריסה.

**SRV-11 · נקודת קצה שהוסרה**
- מצופה: `/app/join_request` מחזיר 404/ברירת מחדל בלי לשבור בילד ותיק.

**SRV-12 · `safeupdate` guard**
- מצופה: אין מיגרציה/RPC עם `UPDATE`/`DELETE` בלי `WHERE` (נדחה בזמן ריצה).

**SRV-13 · `BACKWARD_COMPAT.md` מעודכן**
- מצופה: לכל שים חי יש רשומה; רשומות שהתיישנו הוסרו.

**SRV-14 · סוויפים פועלים**
- אימות: `select * from cron.job_run_details order by start_time desc limit 20` — `expire-sweep` כל דקה, `watch-expire-sweep` כל שעה, בלי כשלים.

**SRV-15 [P0] · רגרסיה: שומרי המחיצה בקהילות/חברים/דיווח**
- הקשר: שמונה השומרים שנוספו ב-`20260727120000`. הבדיקה כולה SQL ואינה כותבת דבר: כל שומר חוזר לפני ה-INSERT הראשון. הרץ אותה אחרי **כל** מיגרציה שנוגעת באחת מהפונקציות האלה.
- קדם: מזהה חשבון בדיקה (`<T>`), מזהה חשבון אמיתי (`<R>`), קוד ההזמנה של קבוצת בדיקה (`<TG>`), קוד של קבוצה אמיתית (`<RG>`), קוד הזמנה אישי של אמיתי (`<RC>`) ושל בדיקה (`<TC>`).
```sql
select
  app_redeem_invite('<T>','<RG>')->>'error'            as test_joins_real_group,   -- invite_invalid
  app_redeem_invite('<R>','<TG>')->>'error'            as real_joins_test_group,   -- invite_invalid
  app_friend_request('<T>','<R>')->>'error'            as test_friends_real,       -- bad_target
  app_friend_request('<R>','<T>')->>'error'            as real_friends_test,       -- bad_target
  app_friend_link_by_code('<T>','<RC>')->>'error'      as test_links_real,         -- bad_code
  app_friend_link_by_code('<R>','<TC>')->>'error'      as real_links_test,         -- bad_code
  app_report('<T>','<R>','qa','QA regression')->>'error' as test_reports_real,     -- bad_target
  jsonb_array_length(app_search_people('<T>','<שם אמיתי>')) as test_sees_real,     -- 0
  jsonb_array_length(app_search_people('<R>','<שם בדיקה>')) as real_sees_test,     -- 0
  jsonb_array_length(app_search_groups('<R>','<שם קבוצת בדיקה>')) as real_sees_tg; -- 0
```
- מצופה: בדיוק קודי השגיאה שבהערות, ואפסים בשלוש הספירות.
- אימות שלא נכתב כלום: `select count(*) from reports where note = 'QA regression'` → 0, ואז ENV-13 → 0 שורות בשלושתן.
- ⚠️ **אל תריץ** את הכיוון `app_report('<R>','<T>')`: הוא **מותר** במכוון (בטיחות של משתמש אמיתי אינה נחסמת), ולכן הוא יכתוב דיווח וחסימה אמיתיים.

**SRV-16 · מסלול תקין באותה מחיצה לא נפגע**
- מצופה: `app_search_people('<T>','QA-')` מחזיר את חשבונות הבדיקה; `app_redeem_invite('<T>','<TG>')` מחזיר `join_status` (`joined`/`pending`/`already`) בלי `error`; בקשת חברות בין שני חשבונות בדיקה עוברת.
- זו הבדיקה שמוכיחה שהשומרים חוסמים רק חוצה-מחיצות ולא שברו את הפיצ'ר.

**SRV-17 · הדגל נחתם על קבוצה חדשה**
- צעדים: `QA-OWNER` יוצר קבוצה מהאפליקציה.
- מצופה: `select name, is_test from groups order by created_at desc limit 1` → `is_test = t` בלי התערבות ידנית.

---

## 17. NFR — ביצועים, i18n, נגישות, יציבות

**NFR-01 [P0] · זמן עלייה קר**
- מצופה: מסך שימושי תוך זמן סביר; אין מסך לבן ארוך; אין קריסת `addViewAt` (בעיה ידועה — לתעד אם חזרה).

**NFR-02 · צריכת סוללה/רשת בשיחה ארוכה**
- מצופה: אין פולינג אגרסיבי כשה-Realtime פעיל.

**NFR-03 · זיכרון בגלילת תמונות**
- מצופה: 6 תמונות × כמה כרטיסים לא מפילים את האפליקציה.

**NFR-04 [P0] · RTL**
- מצופה: כל המסכים מיושרים נכון; ההמבורגר/כפתור סגירה בקצה ההתחלה; המגירה נכנסת מהצד הנכון; חצים ואייקונים לא הופכים כשלא צריך.

**NFR-05 · גופן מוגדל (accessibility font)**
- מצופה: טקסטים לא נחתכים; כפתורים נשארים לחיצים; תקרת הגדלה נאכפת.

**NFR-06 · אין מקף ארוך במחרוזות**
- אימות: `grep '—' mobile/src/i18n/he.ts mobile/src/i18n/en.ts` → אין תוצאות.

**NFR-07 · טקסטים חסרים**
- אימות: אין מפתח שמוצג כ-`home.xxx` גולמי בשום מסך/שפה.

**NFR-08 · צבעים שטוחים בלבד**
- מצופה: אין גרדיאנטים/זגוגיות; טקסט בפופאפים בסגול מלא (לא דהוי).

**NFR-09 · DRY (סקירת קוד)**
- מצופה: אין ליטרל חדש בלי טוקן; אין מימוש שני של סוויפ-למטה (`PullPane` בלבד); אין קומפוננטה משוכפלת.

**NFR-10 · יציבות מחוות**
- מצופה: 50 סוויפים/פתיחות שכבה ברצף לא מייצרים מצב תקוע או שכבה "רפאים".

**NFR-11 · סיבוב מסך / פיצול מסך**
- מצופה: אין קריסה; פריסה מתאוששת.

**NFR-12 · חשבון בדיקת חנות (Apple/Google review)**
- מצופה: מסלול ההתחברות המיוחד עובד מקצה לקצה ומאפשר לבודק לראות תוכן.

---

## 18. E2E — תרחישי קצה-לקצה משולבים

> בכל תרחיש E2E: **כל** החשבונות המשתתפים `is_test=true`, כולל אלה שנוצרים באמצע התרחיש
> (סמן בסוף שלב 3 של האונבורדינג לפי ENV-10, לפני העלאת תמונה). תרחיש שרץ עם חשבון
> לא-מסומן אחד — תוצאותיו פסולות, גם אם "עבד", כי הוא חשף חשבון בדיקה לפול האמיתי.

**E2E-01 [P0] · מקבוצה עד צ'אט**
OWNER יוצר קבוצה עם אישור → משתף קישור → B (מכשיר נקי) פותח → מותקן → נרשם → אונבורדינג מלא → בקשת הצטרפות ממתינה → OWNER מאשר (פוש) → B מקבל בוסט ×3 בפול → A (חבר אותה קבוצה) רואה את B עם צ'יפ קבוצה משותפת → A שולח הזמנה (קרדיט יורד) → B מקבל פוש ומאשר (קרדיט יורד) → שיחה: טקסט, תמונה, אודיו, מיקום, reply → אישורי קריאה → A מסיים שיחה → שני הצדדים מקבלים כרטיס סיום → הגבלת 14 יום מאומתת.

**E2E-02 [P0] · מקישור חבר עד תפוגה**
A משתף `/f/<CODE>` → B מותקן דרך הקישור → קישור חברות + קרדיט ל-A אחרי השלמת פרופיל → B מזמין את A → A לא עונה 10 דקות → שני הצדדים מקבלים כרטיסי תפוגה → הקרדיט של B מוחזר → B יכול להזמין מישהו אחר.

**E2E-03 [P0] · הורדה חופשית עד דחייה**
התקנה אורגנית → אונבורדינג → play → מועמד → הזמנה → הנמען דוחה → פוש `declined` + החזר קרדיט → הגבלת 7 ימים מאומתת → play מביא מועמד אחר.

**E2E-04 · ביטול מול אישור (מיגזע)**
A מזמין את B → שניהם לוחצים בו-זמנית (A ביטול, B אישור) → תוצאה עקבית אחת בשני המכשירים → אין קרדיט אבוד ואין צ'אט חד-צדדי.

**E2E-05 · חסימה מלאה**
צ'אט פעיל → B חוסם → השיחה נסגרת בשני הצדדים → הזוג לא רואה זה את זה שוב אף פעם (בדוק אחרי 24 שעות ואחרי שבוע).

**E2E-06 · דיווח מהצפייה**
A רואה פרופיל פוגעני → מדווח עם הערה → הפרופיל נעלם מיד → חסימה לצמיתות → הדיווח נראה באדמין.

**E2E-07 · שער התראות מקצה לקצה**
B דוחה התראות → יוצא מהפול (A לא רואה אותו) → B מאשר מהגדרות המערכת → חוזר לפול תוך שנייה-שתיים בלי הפעלה מחדש.

**E2E-08 · חלון נוכחות**
B לא פותח את האפליקציה 25 שעות → נעלם מהפול של כולם → פותח → חוזר מיד.

**E2E-09 · יציאה, חזרה, והמשך שיחה**
בזמן צ'אט: A עושה logout → B רואה "כבר לא כאן" → A מתחבר שוב → A גלוי שוב (page2 חוזר free) → אין צ'אט רפאים.

**E2E-10 · מחיקת חשבון תוך כדי הזמנה**
A שולח הזמנה → A מוחק חשבון → B מקבל כרטיס `delete` תקין → B חוזר לזמינות בלחיצה אחת.

---

## 19. נספח — טבלת סטטוס להרצה

| סקציה | מס' מקרים | P0 | עבר | נכשל | הערות |
|---|---|---|---|---|---|
| ENV | 15 | 6 | | | שערי המחיצה: ENV-01/09/10/12/13/15 חוסמים את המחזור |
| ENT | 37 | 9 | | | סימון מיידי לכל חשבון חדש (0.1.2) |
| AUTH | 18 | 5 | | | |
| ONB | 18 | 6 | | | ONB-05 = נקודת הסימון המחייבת |
| GATE | 15 | 5 | | | |
| HOME | 20 | 3 | | | |
| FIND | 24 | 8 | | | FIND-18 = אימות המחיצה בכל מסלול |
| INV | 26 | 9 | | | |
| EXP | 12 | 3 | | | |
| CHAT | 36 | 12 | | | |
| END | 11 | 3 | | | END-04/05: דיווח רק מול חשבון בדיקה |
| COMM | 27 | 14 | | | 🛑 סקציה לא-מוגנת: חוקי `QA-` + COMM-27 חובה |
| SET | 20 | 7 | | | |
| PUSH | 11 | 4 | | | |
| RT | 7 | 1 | | | |
| SRV | 17 | 2 | | | SRV-15 = רגרסיית שומרי המחיצה |
| NFR | 12 | 2 | | | |
| E2E | 10 | 3 | | | |
| **סה"כ** | **336** | **102** | | | |

---

## 20. נספח — למה זה קריטי (רקע לבודק)

אין סביבת staging. אותו מסד נתונים משרת את המשתמשים האמיתיים ואת הבדיקות, והפרדה
מלאכותית אחת מחזיקה את כל המחיצה: סעיף בודד ובלתי-מותנה בתוך `others()` — הפונקציה
שכל מועמדות באפליקציה עוברת דרכה. אין נקודת חנק שנייה.

לכן חשבון בדיקה שלא סומן אינו "בדיקה שרצה בסביבה הלא נכונה" אלא **תקלת ייצור**: הוא
מופיע בפול של אנשים אמיתיים, מקבל מהם הזמנות, נועל אותם לעשר דקות, מוציא להם קרדיט,
ופותח איתם צ'אט. הנזק אינו לנתוני הבדיקה — הוא למשתמשים.

המיגרציה שהקימה את המחיצה (`20260719000000_test_user_partition.sql`) אף מגנה על עצמה:
היא מאמתת שהעוגן קיים בגוף החי של `others()` ונכשלת במפורש אם הגוף השתנה
(`others(): is_test anchor not found -- live body drifted, aborting`). ENV-09 ו-FIND-18
הם המקבילה הידנית לאותה בדיקה: מריצים אותם בתחילת כל מחזור ואחרי כל מיגרציה שנוגעת
ב-`others()`.

---

## 21. נספח — ביקורת סיכון: מטריצת פעולות מול סביבת האמת

ביקורת שנעשתה מול גוף הפונקציות **החי** בשרת (לא מול התיעוד), בשאילתה שבדקה לכל
RPC אם הוא עובר דרך `others()` ואם הוא מזכיר `is_test` בכלל. המסקנה: המחיצה מכסה את
המשחק במלואו, ואינה מכסה את הקהילות, החברים והדיווח. הטבלה היא רשימת כל פעולת
הכתיבה באפליקציה, מי היא יכולה לגעת בו, ומה השליטה שמונעת נגיעה במשתמש אמיתי.

| פעולה | מוגן ע"י המחיצה? | מי עלול להיפגע | השליטה במסמך |
|---|---|---|---|
| `find` / חיפוש אוטומטי | ✅ דרך `others()` | — | ENV-09, FIND-18 |
| `ignore` (דילוג) | ✅ יעד מהמשחק בלבד | — | — |
| `add` (ברודקאסט) | ✅ דרך `others()` | — | FIND-18 |
| זריעת צופה (`app_seed_viewer`) | ✅ דרך `others()` | — | FIND-18 |
| `invite` / `approve` / `decline` / `cancel` | ✅ יעד מהמשחק בלבד | — | — |
| `chat` (הודעות) | ✅ דורש התאמה קיימת | — | — |
| `leave` / `block` / `remove` | ✅ יעד מהמשחק בלבד | — | — |
| `extend` | ✅ | — | — |
| `show_me` / `hide_me` / `pause` / `clear_watch` / `clear_invite` | ✅ עצמי | — | — |
| `profile` / `account` / `age` / `range` / `location` | ✅ עצמי | — | — |
| `logout` / `delete` | ✅ עצמי | — | AUTH-16 (סימון מחדש) |
| `search_people` | ✅ מסונן (2026-07-27) | — | SRV-15 · COMM-02 |
| `friend_request` | ✅ `bad_target` (2026-07-27) | — | SRV-15 · COMM-03 |
| `friend_respond` | ✅ אישור נדחה `bad_request` | — | SRV-15 |
| `friend_link` (`/f/`) | ✅ `bad_code` (2026-07-27) | — | SRV-15 · ENT-16/17 |
| `search_groups` | ✅ מסונן על `groups.is_test` | — | SRV-15 · COMM-10 |
| `redeem_invite` (`/g/` / קוד) | ✅ `invite_invalid` (2026-07-27) | — | SRV-15 · ENT-23/26/28 |
| `respond_join` | ✅ אישור נדחה `no_request` | — | SRV-15 |
| `report` (בדיקה → אמיתי) | ✅ `bad_target` (2026-07-27) | — | SRV-15 · END-04 |
| `report` (אמיתי → בדיקה) | ⚠️ **מותר במכוון** | חסימה + תלונה — וזה הרצוי | 0.1.3 · אין להריץ ידנית |
| `unfriend` / `leave_group` / `cancel_join` | ✅ הפיך ומצמצם ממילא | — | כלי התאוששות ב-0.1.3 |
| `referral` (שיוך) | ⚠️ שיוך נרשם, תשלום נחסם חוצה-מחיצות | — | ENV-12 |
| `bug_report` | ❌ מגיע לאדמין (אין ממה להפריד) | רעש בתור התלונות | 0.1.3 · SET-16 (`QA:`) |
| פעולות אדמין בווב (flip/release/reset) | ❌ פועל על כל שורה שנבחרה | הרס מצב של משתמש אמיתי | 0.1.3 · ENV-11 |

### 21.1 שלוש שכבות ההגנה במסמך
1. **מנע** — כל חשבון מסומן `is_test = true` מיד עם יצירתו (0.1.2), וכל שם נושא `QA-`
   כדי שחיפוש מוגבל-תחילית יהיה אפשרי בכלל (0.1.3 כללים 1–3).
2. **גלה** — ENV-09 (פול המועמדים), FIND-18 (כולל צופים ובוסטים), ENV-13 (חברות /
   קבוצות / הגבלות חוצות-מחיצות), COMM-27 (סגירת הסקציה המסוכנת). כולם P0.
3. **תקן** — נוהל ההתאוששות בסוף 0.1.2 ובסוף 0.1.3. שים לב: **פוש שנשלח אינו הפיך**,
   ולכן שכבה 1 היא ההגנה האמיתית ושכבה 3 היא צמצום נזק בלבד.

### 21.2 מה כן ומה לא מכוסה — הצהרה מסכמת (עדכון 2026-07-27)
- **מכוסה מבנית (בקוד):** כל מסלול המשחק (מציאה, הזמנה, אישור, דחייה, ביטול, תפוגה,
  צ'אט, עזיבה, חסימה, הסרת צופה) דרך `others()`, **וגם** כל מסלולי הקהילות, החברים
  והדיווח דרך שמונת השומרים של `20260727120000`. אין דרך שחשבון בדיקה יגיע למשתמש
  אמיתי או ישלח לו פוש, גם לא בטעות, כל עוד ENV-01 עבר.
- **נשאר בנוהל בלבד (שלושה פריטים):**
  1. **דיווח באג** — מגיע לאותו תור אדמין; אין ממה להפריד אותו, ולכן הכלל הוא תחילית
     `QA:` בטקסט (SET-16).
  2. **פעולות אדמין בווב** — `flipTest` / `release_page1/2` / reset פועלות על כל שורה
     שנבחרה, כי זו כל מטרתן. הכלל הוא לסנן את מסך המשתמשים לסביבת "בדיקה" קודם (ENV-11).
  3. **דיווח בכיוון אמיתי → בדיקה** — מותר במכוון (בטיחות אינה נחסמת). אין להריץ אותו
     ידנית מ-SQL.
- **הכללים של 0.1.3 לא בוטלו** — הם ירדו מ"ההגנה היחידה" ל"שכבה שנייה": הם חוסכים זמן
  (תוצאה שתידחה ממילא), שומרים על נתוני בדיקה מובחנים באדמין, ומכסים את שלושת הפריטים
  לעיל.
- **בדיקת הרגרסיה שמחזיקה את זה:** SRV-15 מריצה את שמונת השומרים ב-SQL אחד בלי לכתוב
  דבר. הרץ אותה אחרי כל מיגרציה שנוגעת ב-`others()`, בחיפושים, בחברים, בקבוצות או
  בדיווח — בדיוק כמו ש-`20260719000000` מאמתת שהעוגן שלה עוד קיים בגוף החי.
